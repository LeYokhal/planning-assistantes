# Planning Assistantes — Cadrage de l'application

> **Version 1.14 — 12/09/2026 (soir).** Complète `PLANNING_ASSISTANTES_BRIEF.md` (référence du contrat MCP Doctolib, § 3.2 et § 9.6) et `WORKING_HABITS.md` (régime de travail, applicable tel quel).
> Statut : **périmètre et décisions produit figés** (C2.1 → C2.12, C3.1 → C3.9, C4.1 → C4.13, C5.1, **C6.1 → C6.10, C6.17 → C6.21, C7.1 → C7.11, C8.1 → C8.14**). **Briques 1a, 1b, 2, 3, 3-quater, 4a, 4b, 7a, 7b, 6a, 6d, 8, 8-bis et 6a-bis livrées — briques 7 et 8 closes** (§ 8, § 14) : le périmètre v1 de l'app est complet, l'existant 2026 des absences est repris, **octobre 2026 est publié dans l'app (v1)**, **l'historique janvier → août 2026 est en production et se lit dans l'app** (huit versions historiques, `/planning/<mois>/historique/`). **En cours** : brique 6 « Interface » — **6a « coquille » livrée le 10/09/2026** (PR #26) et **6d « enveloppe du planning » livrée le 11/09/2026** (PR #29, recettée le 12/09) ; **brique 8 « grille du planning » livrée le 12/09/2026** (PR #31, puis 8-bis PR #32, docs PR #33), recettée le 12/09 sur émulation 1 536 × 864 puis **critère 5 atteint le 12/09 par la principale sur son Chromebook** (brique 8 close, pas de 8-ter) ; **6a-bis « aménagement », réduite, livrée le 12/09** (PR #35, `4b1dd6f`, 1 045 / 57) ; dossier `PLANNING_ASSISTANTES_INTERFACE_v1_6.md`, qui **prime sur ce cadrage pour la brique 6** (C6.11 → C6.16, retours de recette 6a et 6d, sous-briques 6a-bis et 6b-bis). Ordre : ~~8 → 6a-bis~~ → **6b → 6c**. Restent la brique 5 (n8n) et la brique 0 (VoiceDoctolib, dont la valeur monte avec l'horizon de planification). **Régime transitoire C7.6** jusqu'à la bascule de la principale. Reste un point de rétention (§ 10, C2.6).
> Convention : tout ce qui est marqué *(proposition)* est à confirmer au diff plan de la brique concernée. Aucun chiffre n'est estimé.
>
> **Changements v1.13 → v1.14** : **critère 5 de la brique 8 atteint** le 12/09/2026 (la principale, Chromebook, app en direct — 8 close, 8-ter sans objet) ; brique **6a-bis « aménagement » réduite puis livrée** le 12/09 (PR #35, `4b1dd6f`, commit de branche `7ed7a1e` ; sept fichiers, +127 / −16 ; 1 045 tests Python / 57 Node ; aucune migration ; recette déclarée OK, logs propres) ; **C6.18 amendée** (contenu de la 6a-bis : trois points, aménagement 2 560 px retiré) ; **C6.21** (§ 2 : décisions D6a-bis.1 → .3 et A → E) ; § 8 : ligne 6 ; § 12 : préalables du 12/09 soldés (cleanup, captures, exports prouvés), 2 560 px retiré ; § 14 : journal 6a-bis ; § 15 : leçons « aménagement ». Édition ciblée : rien d'autre n'a bougé.
> **Changements v1.12 → v1.13** : briques **8 « Grille du planning »** (PR #31, `2522e97`) et **8-bis** (PR #32, `89885de`) livrées le 12/09/2026, docs PR #33 (`8707429`) ; **C8.1 → C8.14** (§ 2 : les quatorze décisions de la brique 8, dont C8.14 renversée en recette — toutes les briques pleines) ; **C6.18** amendée (poste de référence 1 536 × 864, 1 366 hors critère) ; **C6.20** soldée ; § 6 : ce que la 8 change en production, `localStorage` limité au repli des lignes ; § 8 : lignes 6 et 8 ; § 12 : recette 6d soldée, issus de la recette 8 ; § 14 : recette 6d close le 12/09, journal 8 et 8-bis ; § 15 : leçons « grille ». Édition ciblée : rien d'autre n'a bougé.
> **Changements v1.11 → v1.12** : brique **6d « Enveloppe du planning »** livrée le 11/09/2026 (§ 8, journal § 14) ; **C6.20** (§ 2 : retours de recette 6d — en-tête allégé, filtre à plusieurs noms, onglets qui suivent le mois — **tous portés par la brique 8, en une seule PR**, dont le périmètre s'élargit : pictogrammes et repli des lignes de rôle, repli des semaines, sommaire, circulation ; fiche `PLANNING_ASSISTANTES_BRIQUE8_v0_1.md`) ; § 6 : ce que la 6d change en production ; § 8 : lignes 6 et 8 ; § 12 : issus de la recette 6d ; § 15 : leçons « enveloppe ». Édition ciblée : rien d'autre n'a bougé.
> **Changements v1.10 → v1.11** : brique **6a « Coquille »** livrée le 10/09/2026 et recettée le même jour (§ 8, journal § 14) ; **C6.17** (§ 2 : décisions A → I du diff plan 6a — lien magique → `/connexion/?expire=1`, admin en cinq blocs et `socle` en tête d'`INSTALLED_APPS`, tableau de bord, favicon) et **C6.18** (ordre des sous-briques **6d → 8 → 6a-bis → 6b → 6c**, brique 8 « Lisibilité de la grille » à cadrer sur le Chromebook de la principale) ; **C6.19** (« Mes absences » groupées par mois, 6b — au dossier) ; § 6 : `/`, lien périmé, `500.html` réels ; § 8 : lignes 6 et 8 ; § 12 : PR #25 faite, retours de recette 6a ; § 14 : journal 6a, ligne d'octobre reformulée sans absence nominative ; § 15 : leçons « coquille ». Édition ciblée : rien d'autre n'a bougé.
> **Changements v1.9 → v1.10** : brique **7b « Lecture d'un mois historique »** livrée le 08/09/2026 et recettée le 09/09 (§ 8, journal § 14), sa doc mergée le 09/09 ; **C7.11** (§ 2 : règle des journées de la page historique, extension de C3.2, `_doc` de `periodes_ouverture` amendé) ; § 5.3 et § 6.1 : log et route réels ; § 12 : `est_ouvert` public au backlog, copie du cadrage du dépôt (v1.7) à aligner ; § 15 : leçons « lecture historique ». **Brique 7 close** (quatre PR, aucune migration). Édition ciblée : rien d'autre n'a bougé.
> **Changements v1.8 → v1.9** : brique **7a « Import historique »** livrée le 08/09/2026 (§ 8, journal § 14) et **historique janvier → août 2026 importé en production** le même jour ; décisions **C7.7 → C7.10** (§ 2 : mois déjà versé, forme d'une version historique, audit, découpage 7a / 7b) ; § 5.3 et § 6.3 : événement et écran réels ; § 12 : backlog de la 7a et sujets réservés à la version qui suivra la brique 6 ; § 15 : leçons « import historique ». Édition ciblée : rien d'autre n'a bougé ; les décisions de la brique 6 prises le 08/09 (C6.11 → C6.16) vivent dans le dossier interface v1.2 jusqu'à sa Phase 6.
> **Changements v1.7 → v1.8** : soirée du 07/09/2026 — **brique 6 « Interface »** cadrée (C6.1 → C6.10 : Claude Design, périmètre, supports, admin habillé, tableau de bord par horizon, barre haute, « Mes jours » en grille avec codes et marqueurs d'effectif — amende C4.8) ; **brique 7 « Historique 2026 »** cadrée (C7.1 → C7.5 : import historique depuis les entrées « Présence » de Notion, lues en colonnes structurées) ; **octobre 2026 mis en place dans l'app sans code** (C7.3) après découverte que Notion portait les plannings de septembre et d'octobre, saisis un à trois mois à l'avance ; **C7.6** régime transitoire (principale désignée, invitation différée, ré-import de fin de mois) ; PR #19 (3-quater) et #20 (docs) reportées ; § 1.1 / § 1.3 / § 8 / § 11 / § 12 / § 14 / § 15 mis à jour ; § 6 inchangé jusqu'à l'implémentation de la 6.
> **Changements v1.6 → v1.7** : brique 3-quater livrée le 07/09/2026 (§ 8, journal § 14) — reprise exceptionnelle de l'existant Notion 2026 par un écran d'import dans l'admin ; **C3.9** amende C3.8 ; **C5.1** : la paie envoie dates + type de paie, la comptable décompte — `jours_comptes` devient un indicateur interne (amende C3.1 en statut, § 1.2 et § 5.2 en portée) ; § 11 : étalon de paie acté sur les bulletins de janvier → août ; § 3.2 flux 4 ; § 6.3 écran d'import ; § 7 : ce que la formule C3.1 ne modélise pas ; § 12 : backlog ; § 15 : leçons « reprise ».
> **Changements v1.5 → v1.6** : brique 4b livrée le 07/09/2026 (§ 8, journal § 14) ; R4a-1 acté avec l'écart E1 (§ 11, § 14) ; décisions C4.5 → C4.13 (§ 2) ; C2.7 et C2.8 tranchées ; § 1.1 / § 1.3 : publication, « Mes jours » ; § 3.1 / § 3.2 : flux 2 complet, flux 3 avec conflit ; § 3.3 : `meta.imports` ; § 4 : `PlanningVersion` figée, `version_de_base` non nul, actions d'audit 4b ; § 5.2 / § 5.3 / § 5.5 : surfaces 4b, logs, variable `N8N_PLANNING_WEBHOOK_URL` ; § 6.1 / § 6.2 / § 6.5 : routes et événements réels ; § 7 : conflit ; § 12 : backlog 4b ; § 15 : leçons « recette croisée ».
> **Changements v1.4 → v1.5** : brique 4 découpée en **4a / 4b**, 4a livrée le 06/09/2026 (§ 8, journal § 14) ; décisions C4.1 → C4.4 (§ 2) dont l'**amendement du § 1.2** sur le type d'absence ; § 1.1 : page planning servie par l'app, versions ; § 3.1 / § 3.2 : app `planning`, flux 2 en place hors publication ; § 3.3 : contrat `DATA` / `STATE` réel ; § 4 : `PlanningVersion` figée ; § 5.2 / § 5.3 : sept surfaces, actions d'audit 4a ; § 6.1 / § 6.4 : routes et garde-fous réels ; § 7 : règles strictes vérifiées, exclusivité ; § 11 : critère différé R4a-1 ; § 12 : hygiène `version_de_base` ; § 15 : leçons « page, brouillon et référence ».
> **Changements v1.3 → v1.4** : brique 3 livrée le 03/09/2026 avec ses sous-briques 3-bis (04/09) et 3-ter (06/09) (§ 8, journal § 14) ; décisions C3.1 → C3.8 (§ 2) dont l'abandon de la migration Notion des absences au profit d'une saisie manuelle ; § 1.1 / § 1.3 : espace salariée en place, la principale y a accès ; § 3.1 / § 3.2 : composants et flux 3 en place, flux 4 côté app ; § 4 : `TypeAbsence`, `AbsenceSalariee` (avec `jours_retenus`), `Compte.email_en_attente`, actions d'audit 3 ; § 5.1 : changement d'adresse livré, HSTS à un an ; § 5.3 : suppressions d'absence journalisées, la leçon du `SET_NULL` ; § 5.5 : deux variables ; § 6.2 / § 6.3 / § 6.5 : surfaces réelles ; § 7 : formule des jours comptés, périodes d'ouverture datées, répartition par mois ; § 10 / § 11 / § 12 : points restants et backlog réordonnés ; § 15 : leçons « paie, audit et recette ».
> **Changements v1.2 → v1.3** : brique 2 livrée le 31/08/2026 avec ses sous-briques 2-bis, 2-ter et 2-quater ; C2.12 (IP cliente = `X-Real-IP`, mesuré) ; § 4 : contrainte et code de `Personne`, `CompteurDebit`, actions d'audit 2 ; § 5.1 : changement d'adresse → brique 3 ; § 5.2 / § 6.5 : limitation de débit en place ; § 7 : gardes de lecture de la fiche ; § 12 : débit sorti du backlog, hygiène post-recette ; § 15 : leçons « proxy, mesure et recette ».
> **Changements v1.1 → v1.2** : brique 1b livrée le 31/08/2026 ; décisions du diff plan 1b v2 intégrées (C2.10, C2.11) ; § 4 : `ImportPresences` et `VerrouImport` ; § 5.5 : cinq variables ; § 6.5 / § 6.6 : routes et comportement réels ; § 15 : leçons de la recette 1b.
> **Changements v1.0 → v1.1** : ajout du § 14 ; C2.9 tranché (a, webhook n8n → Gmail) ; décisions du diff plan 1a intégrées ; § 15 leçons de déploiement Railway.

---

## 1. Périmètre v1

### 1.1 Ce que l'app fait
- **Présences des praticiens** : import du payload `consulter_jours_travail` (contrat brief § 3.2), stockage brut, invariant de recompte, écran « présences du mois ». **En place depuis la 1b** par le chemin fichier ; chemin endpoint câblé, inactif jusqu'à la brique 0.
- **Personnes et contrats** : qui est planifié, rôle, heures hebdomadaires, jours fixes, agenda Doctolib apparié, couleur. Reprend le rôle de la case `Planning` de ☎️ Fiche personnel. **En place depuis la 2.**
- **Absences des salariées** : demande par la salariée, validation par l'assistante principale ou le cabinet pour les types *demandés*, effet immédiat pour les types *déclarés* ; jours d'école des étudiantes comme absences de type `Ecole` ; jours comptés pour la paie, corrigeables par la validatrice ; données de paie du mois servies à n8n. Reprend le rôle des pages « Absence » de 📅 Agenda cabinet, **sans migration** : l'existant est ressaisi à la main (C3.8). **En place depuis la 3.**
- **Planning mensuel** : la page actuelle (proposition, drag & drop, compteurs, notes, impression) est **une vue de l'app depuis la 4a** : `DATA` calculé côté serveur, moteur JS isolé et testé, versions à chaque enregistrement (409 / 422), copie HTML autonome, plus de brouillon navigateur ; **publication d'une version (4b)** : revérification serveur, audit, webhook `planning.publie`, « Mes jours » pour chaque salariée, conflit absence ↔ planning publié signalé (jamais bloquant). Remplace les entrées « Présence » saisies à la main dans 📅 Agenda cabinet — ~~aucun lecteur, aucune migration~~ : **l'historique janvier → août 2026 sera repris par un import historique (C7.1, brique 7)** ; les mois à venir planifiés dans Notion entrent par la page (fichier au format export, « Importer… », C7.3) ; ré-import de fin de mois jusqu'à la bascule (C7.6).
- **Notifications** : v1 = le mail mensuel à la comptable (données de paie), via n8n + Gmail. **La donnée est prête depuis la 3** (endpoint de paie) ; l'envoi reste la brique 5. Le reste est en backlog (§ 12).

### 1.2 Ce que l'app ne fait pas
- **Aucune écriture Doctolib.** Les agendas des praticiens sont lus (présences), jamais modifiés. Les outils de mutation MCP restent hors périmètre.
- **Aucune donnée patient**, nulle part : ni en base, ni en logs, ni en webhook. `consulter_planning` (PHI) n'est pas utilisé.
- **Aucune donnée RH sensible hors besoin** : pas de n° de sécurité sociale, date de naissance, téléphone personnel, IBAN, adresse — ces champs restent là où ils sont (Notion, Google Sheets). **Le type d'une absence et sa précision ne sortent jamais de l'application** : ni dans l'audit, ni dans les logs, ni dans un webhook — **une exception nommée (C5.1, 07/09/2026)** : les données de paie servies à n8n pour la comptable portent la **catégorie de paie** (congé payé, sans solde, maladie, événement familial, enfant malade) des seuls types `paie`, celle qui figure sur chaque bulletin ; la précision, jamais (§ 5.2). **Amendement C4.1 (06/09/2026)** : le libellé du type figure dans le `DATA` de la page planning, donc à l'écran (rôles `cabinet` et `principale`) **et dans la copie HTML autonome téléchargée** ; il ne figure ni dans `STATE`, ni dans les versions, ni dans l'export JSON.
- **Pas de paie** : l'app fournit les données d'absence à la comptable, elle ne calcule ni bulletin ni solde de congés (`fdp-cabinet-k` reste une skill séparée).
- Le « entre autres » (modules futurs, possiblement avec données de santé) est en **backlog** (§ 12), sans décision.

### 1.3 Utilisateurs et rôles
| Rôle | Qui | Voit | Fait |
|---|---|---|---|
| `cabinet` | Yohan | tout : personnes, absences avec types, planning, imports, audit | administre les personnes, valide ou refuse les demandes — **seul à décider d'une demande émise par la personne concernée** (C3.4) —, corrige les jours comptés, publie, déclenche un import |
| `principale` | l'assistante principale, qui construit le planning | **tout ce qui concerne l'agenda, motifs d'absence compris** (décision C2.3b), **et son propre espace de salariée** (3-bis) | édite et enregistre le planning, publie, valide ou refuse les demandes d'absence des autres, corrige les jours comptés, demande ses propres absences |
| `salariee` | chaque assistante et secrétaire | ses jours publiés (« Mes jours », 4b : dates, journée, avec qui ; **brique 6, C6.8 / C6.9** : grille de mois, ses propres absences, marqueurs d'effectif du cabinet), ses absences et l'état de ses demandes (« son espace ») | demande ou déclare ses absences, annule une demande en attente, change son adresse de connexion |

Rôles extensibles (décision C2.1). Les praticiens n'ont pas de compte en v1. **Implémenté en 1a** : les trois rôles sur `Compte`. **1b** : `role_requis`. **2** : fiche dans l'application, comptes des salariées créés en masse. **3** : premier espace du rôle `salariee` (`/mes-absences/`, `/mon-profil/`), écran de décision pour `principale` et `cabinet`, admin des absences pour `cabinet`. **3-bis** : l'espace personnel est ouvert à `principale` — un compte n'a qu'un rôle, et la principale pose aussi ses congés. Le rôle `cabinet` reste exclu de cet espace (il n'a pas d'absence). La désignation de la principale est un geste dans l'admin (rôle sur son compte), à dater au § 14 quand il a lieu. **07/09/2026 : principale désignée = Amandine ; invitation différée à la finalisation de l'app (fin de brique 6), régime transitoire C7.6.**

---

## 2. Décisions actées

| # | Question | Décision | Conséquences |
|---|---|---|---|
| — | Forme cible | application web hébergée, base partagée (brief § 9.1, confirmé) | Railway + PostgreSQL ; la page HTML devient un front servi par l'app |
| — | Présences Doctolib | **option B** : endpoint `POST /admin/presences` sur le serveur MCP, payload de `consulter_jours_travail` **tel quel** (brief § 9.2 / 9.6) | brique 0, cadrée et exécutée **dans VoiceDoctolib** ; **depuis la 1b** le client côté app existe, inactif sans ses variables ; la brique 0 s'aligne sur `DOCTOLIB_PRESENCES_URL`, `DOCTOLIB_PRESENCES_SECRET`, en-tête `X-Presences-Secret`, corps `{"date","date_fin","praticien":"tous"}`, 200 = payload tel quel |
| C2.1 | Posture sécurité et identité | **option 3 « Planning + fondations »** | lien magique, rôles extensibles, journal d'audit dès la brique 1, type d'absence isolé et jamais journalisé, date de rétention prévue dans le modèle |
| C2.2 | Où vit la logique de règles et de proposition | **option A : dans le navigateur** | trois garde-fous (§ 6.4) ; le serveur calcule `DATA`, stocke `STATE` en versions |
| C2.3 | Validation des absences | **selon le type** : *demandés* (congé payé, congé sans solde) → demande, validée ou refusée ; *déclarés* (tous les autres) → effectifs dès la saisie | **implémenté en 3** : `TypeAbsence.categorie`, statuts `en_attente` / `validee` / `refusee` / `annulee` / `declaree`, `effective` = propriété calculée ; le conflit après publication attend la brique 4 (l'objet « planning publié » n'existe pas avant) |
| C2.3b | Le rôle `principale` voit-il les motifs | **oui, tout** | pas de vue anonymisée pour ce rôle ; minimisation appliquée aux autres sorties (§ 5.2) |
| C2.4 | Stack | **Django + PostgreSQL** sur Railway, page actuelle comme front | comptes, sessions, migrations, admin ; lien magique par django-sesame ; API n8n |
| C2.5 | E-mail de connexion | **choisi par la salariée** | création protégée par invitation ; **changement d'adresse livré en 3** (§ 5.1) |
| — | Durée d'une absence (paie) | **jours de travail selon son contrat**, pas jours calendaires | formule C3.1 ; le planning n'existant pas avant la brique 4, c'est le contrat qui fait foi |
| — | Automatisation à reproduire | **une seule en v1** : le mail mensuel à la comptable | endpoint de paie **livré en 3** (§ 6.5), workflow n8n en brique 5 |
| C2.7 | Qui publie un planning | **`principale` et `cabinet`, pas de dépublication** (tranché 06/09, décision F de la 4b) | audit `planning_publie` à chaque publication ; une correction = nouvelle version puis publication, qui supersède |
| C2.8 | Copie HTML autonome, brouillon navigateur | copie conservée ; brouillon `localStorage` supprimé — **appliqué en 4a** | la copie reste celle de la dernière version enregistrée, pas de variante « copie de la publiée » (4b, décision N) |
| C2.9 | Canal des e-mails de connexion et d'invitation | **(a) webhook n8n → Gmail** | **en place depuis 1a** ; aussi le mail de confirmation de changement d'adresse (3) |
| C2.10 | Tâche de fond des tirs endpoint | thread + verrou en base, péremption 15 minutes | **en place depuis 1b** |
| C2.11 | Secrets et en-têtes des échanges avec n8n | entrant `N8N_API_SECRET` / `X-Api-Secret` ; sortant `N8N_WEBHOOK_SECRET` / `X-Webhook-Secret`, une URL par flux | **en place depuis 1b** ; la 3 ajoute `N8N_ABSENCE_WEBHOOK_URL`, même secret, même en-tête |
| C2.12 | IP cliente derrière le proxy Railway | `X-Real-IP`, réécrit par Railway — établi par mesure | **en place depuis 2-quater** |
| **C3.1** | **Formule des jours comptés** | par semaine lundi→dimanche, `min(J, max(0, B − F))` : `J` = jours d'absence tombant sur un jour d'ouverture du cabinet, fériés exclus ; `B` = briques du contrat (39 h → 4, 35 h → 4, 27 h → 3) ; `F` = fériés tombant sur un jour d'ouverture. Personne à jours fixes : ses jours fixes hors fériés. Contrat incomplet : 0 et signal, jamais de valeur supposée | § 7 ; formule Notion « Durée » jamais fournie — le critère de recette différé (S3b, § 11) est le filet. Le plafond s'applique **une fois par semaine**, et les jours retenus sont **datés** (C3.6) |
| **C3.2** | **Jours d'ouverture du cabinet** | **datés**, dans `regles.json` : mardi→samedi à l'origine, **lundi→vendredi à partir du lundi 5 octobre 2026** ; servent **uniquement** au calcul de paie, le planning lit Doctolib | bascule calée sur un lundi pour qu'aucune semaine ne soit à cheval ; un samedi ouvert ponctuellement se rattrape en paie par correction manuelle |
| **C3.3** | **Demi-journées** | journée entière à la saisie ; la validatrice pose 0,5 par **correction manuelle** ; les montants sont en décimal | aucun champ de saisie en demi-journée ; une absence corrigée à 0,5 reste **un jour bloqué au planning** (divergence assumée, à ne pas « corriger » en brique 4) |
| **C3.4** | **Règle K** | une demande dont la personne concernée est le décideur ne peut être tranchée que par `cabinet` — assise sur la **personne**, pas sur l'auteur de la saisie (qui part en `SET_NULL`) | la principale ne valide jamais ses propres congés |
| **C3.5** | **Rétention (mécanisme)** | réglage `RETENTION_ABSENCES_JOURS`, **absent = aucune purge**, `a_effacer_le` laissé nul, rattrapé par la commande de purge quand le réglage apparaît | ne bloque pas le déploiement (contrairement à `regles.json`) : une durée juridique non rendue n'est pas une condition de fonctionnement. La **valeur** reste C2.6 (§ 10) |
| **C3.6** | **Paie : plage et répartition** | la paie filtre sur le **mois calendaire** (jamais `plage_mois`, outil du planning en semaines complètes) ; **chaque mois reçoit sa portion** d'une absence à cheval, lue sur les **jours retenus datés** figés au calcul ; **on ne recoupe jamais une absence pour relancer le calcul** (le plafond hebdomadaire s'appliquerait deux fois) ; absence corrigée : prorata au demi-jour inférieur, reliquat au mois du premier jour retenu | défaut trouvé au checkpoint diff, avant tout merge : quatre jours facturés deux fois. Voir § 15 |
| **C3.7** | **Client HTTP n8n sortant** | factorisé dans `socle/client_n8n.py` (trois appelants : mail, import, absence) ; l'import `requests` est **conservé** dans les deux appelants historiques, avec commentaire | douze tests patchent `<module>.requests.post` ; le retirer les ferait tomber en `AttributeError`. Factorisation prévue en 5, faite en 3 |
| **C3.8** | **Existant Notion des absences** | ~~aucune migration : ressaisie à la main~~ — **amendée par C3.9** | décision du 01/09/2026, tenue pour septembre à la main jusqu'au 07/09 |
| **C3.9** | **Reprise exceptionnelle de l'existant 2026** (07/09/2026) | **un** import depuis un fichier JSON produit hors de l'app (lecture Notion, arbitrages, fusion des jours consécutifs), par un **écran d'admin** en deux temps — rapport d'analyse, puis confirmation, écriture atomique, idempotente (« déjà présente »), sans webhook ni crochet de conflit (les conflits sont listés dans le rapport) ; Notion devient archive ; l'écran reste disponible mais n'a plus de source | brique 3-quater ; **la fiche de paie prime sur Notion** en cas d'écart (17 arbitrages, § 14) ; les données nominatives du fichier n'entrent ni dans le dépôt ni dans un prompt |
| **C5.1** | **Ce que la paie envoie à la comptable** (07/09/2026) | **dates + catégorie de paie**, par salariée et par absence effective d'un type `paie` ; la comptable décompte selon sa convention (jours ouvrables pour CP et maladie, jours réellement non travaillés pour le sans-solde — constaté sur les bulletins, § 11) ; **`jours_comptes` devient un indicateur interne** (compteurs, corrections, suivi), plus une donnée de paie | amende C3.1 en statut, § 1.2 et § 5.2 en portée ; aucune formule nouvelle ; à implémenter dans le mail de la brique 5 (l'endpoint de paie sert déjà les dates ; la catégorie de paie s'y ajoute) |
| **C4.1** | **Type d'absence dans la page planning** | **comme le gabarit** : codes CP / MAL / SS à l'écran, dans la copie HTML et son `DATA` embarqué ; jamais dans `STATE`, les versions, l'export JSON, l'audit, les logs, les violations, `/api/erreurs/` | décision Yohan du 06/09, prise contre l'avis de Claude ; amende le § 1.2 ; isolée dans une fonction (`libelle_conge`) pour être réversible ; sept surfaces testées (§ 5.2) |
| **C4.2** | **Découpage de la brique 4** | 4a = page servie, moteur isolé, vérification double, versions, copie, erreurs ; 4b = publication, conflit absence / planning publié, « mes jours publiés » (`salariee`, minimale) | champs de publication de `PlanningVersion` créés en 4a, inertes |
| **C4.3** | **Décision de proposer** | jamais un drapeau du `state` : la page propose si et seulement si la version servie est `0` | défaut trouvé en revue v2 : `initialise` retiré par le nettoyage aurait fait re-proposer par-dessus une version enregistrée ; `initialise` / `modifie` sortent du contrat |
| **C4.5** | **Sémantique de la publication** | publier = poser `publiee / publie_le / publie_par` **sur la dernière version**, numéro explicite dans l'URL, **409** si ce n'est plus la dernière ; « version publiée du mois » = dernière `publiee=True` par `numero` ; republier → 200 `deja_publiee`, sans écriture ni audit | pas de copie figée, pas de nouvelle ligne ; écriture en un seul `UPDATE … WHERE NOT publiee AND NOT EXISTS(numero supérieur)` (seul point de sérialisation, comme la contrainte unique de 4a) |
| **C4.6** | **Revérification à la publication** | `verifier(DATA recalculé, state)` ; **422 si violation, rien n'est écrit** ; `verifications` = `{verifie_le, imports[{id, empreinte}], nb_briques}` | une absence devenue effective ou un mouvement Doctolib **avant** publication est attrapé ici ; **après**, c'est C4.7 |
| **C4.7** | **Conflit absence ↔ planning publié** | détecté à **l'entrée en état effectif**, quel que soit le chemin (déclaration, validation, admin) ; conflit = brique de la salariée, **tout slot**, sur une date de l'absence, dans la version publiée de chaque mois dont la plage contient la date ; **types bloquants seulement** ; audit `absence_conflit_publication` + webhook `absence.conflit` + bandeau `/absences/` ; **jamais bloquant**, aucune persistance (recalculé au rendu) | le crochet vient après `save()`, audit et webhook existants, et ne lève jamais ; limite assumée : changer les dates ou le type d'une absence **déjà** effective ne resignale pas (transition seulement) |
| **C4.8** | **« Mes jours »** | page serveur minimale, **sans `DATA`** : pour la personne connectée, dates, journée (J / C, heures sup), libellé du slot (`Prénom Nom` du praticien, Secrétariat, Sureffectif, Administratif) ; **toute la plage** de la version publiée, jours du mois voisin en italique ; pour une date portée par deux versions publiées, **celle du mois calendaire fait foi** | `salariee` et `principale` (pour elle-même) ; `cabinet` → 403 ; compte sans personne → message, jamais 500. **Amendée par C6.8 et C6.9 (brique 6)** : la page lit aussi les absences de la personne connectée et les marqueurs d'effectif rangés dans la version ; toujours sans `DATA` |
| **C4.9** | **Brique orpheline (R4a-1-E1)** | la page dessine une ligne « hors présence » dans la case-jour dès qu'une brique pointe un slot praticien non présent (« absent ce jour-là », « jour fermé », « inconnu ») : brique visible, retirable, déplaçable, jamais une cible de dépôt ; pas de nettoyage automatique ; jours **affichés** seulement (un jour hors colonnes reste au chemin export JSON) | défaut vu en R4a-1 : une violation `praticien_absent` que l'utilisatrice ne pouvait pas corriger |
| **C4.10** | **`version_de_base` non nul** | `PositiveIntegerField(default=0)`, migration `planning.0002` (`RunPython` NULL → 0 puis `AlterField`) | hygiène § 12 faite ; 0 ligne touchée en production |
| **C4.11** | **Webhook de publication** | nouveau flux, nouvelle URL `N8N_PLANNING_WEBHOOK_URL` (C2.11), même secret, même en-tête ; corps `{evenement, mois, numero, nb_briques, publie_par_id, lien, horodatage}` ; absente = muet | **non posée** sur Railway : le workflow est brique 5 ; chaque publication journalise « webhook planning non configure » d'ici là |
| **C4.12** | **Webhook de conflit** | événement `absence.conflit` sur l'URL absence existante ; corps absence + `conflits: [{mois, numero, dates}]` — ni slot, ni nom, ni type | même destinataire que les autres événements d'absence ; le workflow « Absences (réception) » le transmet sans branche |
| **C4.13** | **Cycle d'import** | `absences/` importe `planning.conflits` **dans la fonction** (patron `presences/verrou.py`) ; `planning/webhooks.py` n'importe jamais `services` (le comptage lui est passé) | `planning.donnees` importe `absences.services` : le cycle serait direct |
| **C4.4** | **Exclusivité** | stricte **sur les cases praticien** (exclusive ailleurs, non-binôme chez un exclusif = violation) ; le sureffectif reste admis pour une exclusive, comme le gabarit validé le faisait (étape 5 de `proposer`) | constaté à l'implémentation : l'interdire rendait la proposition inenregistrable |
| **C6.1** | **Maquettes de la brique 6** (07/09/2026) | dans **Claude Design**, trois parcours (salariée téléphone, principale PC, cabinet) ; la **grille du planning est représentée depuis une capture, jamais redessinée** | charte capturée depuis le site du cabinet ; export HTML des maquettes sous `docs/maquettes/` (chemin à fixer), sans donnée réelle ; le diff plan traduit, et peut refuser un détail impossible |
| **C6.2** | **Périmètre de la brique 6** | coquille et navigation, accueils, espace salariée, écrans d'absences, **enveloppe** de la page planning (en-tête d'état, barre d'outils, menu « Plus », toasts), habillage de l'admin ; **intouchés : grille, `moteur.js`, contrats `DATA` / `STATE`, règles, droits** — `role_requis` reste la seule garde, les sept surfaces de confidentialité restent vertes | découpage 6a coquille / 6b espace salariée / 6c absences et données / 6d enveloppe du planning ; aucune migration attendue ; dossier `PLANNING_ASSISTANTES_INTERFACE_v0.md` |
| **C6.3** | **Supports** | `salariee` : **téléphone d'abord** (380 px) ; `principale` et `cabinet` : **PC** (≥ 1 280 px) ; la page planning n'a pas de vue mobile (bandeau « écran large ») | onglets bas sur téléphone, barre haute sur PC |
| **C6.4** | **Admin Django** | **habillé** (charte, en-tête, index regroupé en quatre blocs, lien « Retour à l'app ») par `admin/base_site.html` et une feuille de style ; **zéro vue réécrite** | gardes, tests et modèles en lecture seule inchangés |
| **C6.5** | **Page d'arrivée** | `principale` / `cabinet` : **tableau de bord minimal** ; `salariee` : **« Mes jours »** | l'accueil à tuiles disparaît pour les deux premiers rôles |
| **C6.6** | **Navigation PC** | **barre haute fine** : Planning · Absences · Données (Présences, Personnes) · avatar (espace personnel, Administration pour `cabinet`, déconnexion) | la grille garde toute la largeur ; pas de barre latérale |
| **C6.7** | **Jours comptés côté salariée** | **conservés tels quels** dans « Mes absences » | décision Yohan contre la proposition de les masquer (C5.1 en fait un indicateur interne) |
| **C6.8** | **« Mes jours »** | **grille de mois**, codes **J / JC / A / A ? / E / F** (journée, journée courte, absence effective, demande en attente, école, férié), fiche du jour au toucher (avec qui, horaires, heures sup, état d'une absence), aujourd'hui encadré, jours du mois voisin en pointillé ; **affiche ses propres absences** — effectives et en attente | **amende C4.8** : la page lit aussi `AbsenceSalariee` de la personne connectée ; aucun nom d'une autre salariée ; le type n'apparaît que dans sa propre fiche du jour ; maquette comparative du 07/09 (`mes_jours_v2_grille.html`) |
| **C6.9** | **Marqueurs d'effectif** | dans chaque case-jour de « Mes jours » : **− rouge = sous-effectif** (un praticien présent sans assistante dans sa colonne — définition du moteur), **+ vert = sureffectif** (au moins une brique dans le slot Sureffectif), même les jours où la personne ne travaille pas ; **calculés à la publication** (C4.6 recalcule déjà `DATA`) et rangés jour par jour dans `verifications` ; « Mes jours » ne lit toujours que la version | versions déjà publiées complétées par une commande unique ; versions historiques (C7.1) sans marqueur tant que leurs présences ne sont pas importées |
| **C6.10** | **Tableau de bord** | carte Planning en **liste par horizon** — *À venir* (tous les mois ayant une version, publiée ou non), *En cours*, *Passés* repliés —, « Préparer un mois », **« Présences Doctolib importées jusqu'au … »** ; demandes à décider (règle K rendue visible, pas par un 403) ; Données (dernier import, fenêtre ; « Importer un fichier » pour `cabinet`) | motivé par le rythme réel : la principale livre un mois **un mois à l'avance** (novembre rédigé en septembre, envoyé le 1er octobre), parfois **plusieurs mois** (vacances) ; d'où l'importance de la brique 0 |
| **C6.17** | **Décisions de la 6a** (10/09/2026, diff plan v1 → v2) | lien magique refusé → **302 `/connexion/?expire=1`** avec bandeau, même réponse pour tout échec (A-1) ; admin : attributs sur `admin.site`, `base_site.html` + `index.html` étendant le natif, **`socle` en tête d'`INSTALLED_APPS`** (sinon jamais trouvés), barre latérale désactivée, **cinq blocs** (le § 4.9 du dossier omettait `presences`), « Groupes » masqué mais servi, « Comptes et personnes » (libellé réel) (B, H) ; trois feuilles `socle/static/socle/` + `@font-face` dupliqués (C-1) ; `/` reste la route `accueil` = aiguillage, module pur `socle/tableau_de_bord.py`, `accueil.html` supprimé, h1 « Tableau de bord », « Bonjour Prénom » dans le menu avatar, pastilles du § 2, « manquantes » dès un jour non couvert hors historique, `Max(fin)`, 5 demandes + « Voir tout » (D) ; lien Administration sur **le rôle** `cabinet`, garde `is_staff` intacte (E-a1) ; favicon = dent du logo du site sur bleu charte (E-b4) ; blocs `navigation` des pages 6b / 6c / 6d intouchés (F-1) ; test « base inchangée » supprimé, remplacé par un test d'isolement de la page planning (G-1) ; onglets bas **par rôle**, sans `@media` (I-1) | amende C6.4 (cinq blocs) ; processeur de contexte `socle.contexte.coquille` avec garde anonyme et `SimpleLazyObject` — coûts figés par test (`/planning/<mois>/` 9 / 4, `/admin/` 3, anonymes 0) |
| **C6.18** | **Ordre des sous-briques et brique 8** (10/09/2026, recette 6a) | **6d → 8 « Lisibilité de la grille » → 6a-bis « aménagement » → 6b → 6c** ; la brique 8 se cadre sur des captures du **poste réel de la principale** (Chromebook 15,6", ~~1 366–1 536 px utiles~~ **1 536 × 864 — 1 920 × 1 080 à 125 %, poste de référence fixé le 12/09/2026 ; la largeur 1 366 sort du critère de recette**), jamais sur l'écran de 2 560 px de Yohan ; `moteur.js` et le contrat `DATA` / `STATE` restent intouchés, `styles.css` (grille) et `page.js` (rendu, glisser-déposer) entrent dans son périmètre | la page planning est l'outil quotidien de la principale ; C7.6 inchangé (invitations à la fin de la 6) ; **amendée le 12/09 (soir)** : la 6a-bis est **réduite** à « Ajouter » en bouton, menu avatar fermé au clic extérieur, coût constant du tableau de bord — ~~aménagement à grande largeur (2 560 px)~~ retiré : l'effet « vide » est propre à l'écran de Yohan, l'utilisatrice quotidienne est à 1 536 px ; à rouvrir seulement sur retour de la principale (C6.21) |
| **C6.21** | **Décisions de la 6a-bis** (12/09/2026, réduite) | **D6a-bis.1** « Ajouter » de l'index d'admin en **pilule bleue pleine, petite** (`a.ajout`, `administration.css`, variables de cette feuille seulement — `commun.css` jamais chargée dans l'admin), une **rangée** par modèle (`div.rangee` : ligne + bouton côte à côte) ; « Importer un fichier » intouché ; **D6a-bis.2** menu avatar (`<details class="avatar">`) fermé au **clic extérieur et à Échap** par un **script inline dans `base.html`, juste après `</details>`, dans la barre, hors du bloc `scripts`** (`id="script-avatar"`) : toutes les pages, planning comprise, `page.js` intouché ; Échap sur la page planning ferme le menu **et** vide le filtre si les deux sont actifs (écart assumé) ; **D6a-bis.3** tableau de bord à **coût constant** : une requête sur toutes les versions (`order_by("mois", "-numero")`, `defer("state")`) regroupée par mois, `_ligne` sans requête ; couverture Doctolib par les **plages `debut → fin` des imports réussis** (clés ISO, `payload` jamais lu) — propriété déjà garantie par `presences/lecture.py:185-193` (un payload qui ne couvre pas exactement la fenêtre est refusé) ; `imports_par_date` intouché pour `/presences/` et le planning ; **coût figé par test : 8** (cabinet) / **9** (principale rattachée), contre 8 + 2 × L avant (~28 en production, +2 par mois versionné) | décidé point par point sur le rapport de Phase 1 (`rapport_phase1_6abis_4b828c8.md`, mesure 12 / 22 / 34 pour L = 2 / 7 / 13) ; diff plan v1 → revue technique (7 écarts, 2 bloquants : compte des `<script` = 6, clés ISO) → v2 vérifiée par empreinte |
| **C6.20** | **Retours de recette 6d et périmètre de la brique 8** (11/09/2026) | (1) l'en-tête de la page planning est **allégé** : pastille « Données Doctolib du … » et sélecteur « Planning individuel » retirés (C6.12 révisée sur ce point), pastille de version visible seulement hors état stable (D8.2 **actée le 12/09** : masquée sur « Publiée (vN) », C8.2) ; (2) le **filtre du panneau accepte plusieurs noms**, un genre à la fois (assistantes ou praticiens) ; (3) les **onglets de la barre suivent le mois** de la page affichée, depuis toutes les pages qui portent un mois ; (4) **tout cela entre dans la brique 8**, avec les pictogrammes et le repli des lignes de rôle, le repli des semaines, le sommaire de semaines, les bandes collantes, le glisser-déposer assisté et la lisibilité — **une seule PR** (D8.1), en lots ordonnés ; ~~la fiche `PLANNING_ASSISTANTES_BRIQUE8_v0_1.md` prime pour la brique 8~~ **livrée le 12/09/2026 : C8.1 → C8.14 ci-dessous ; fiche v0.3 (clôture)** | décidé par Yohan sur la capture de l'en-tête 6d ; la 6a-bis attend la 8 — ✅ 12/09 |
| **C8.1** | **Une seule PR pour la brique 8** (11/09/2026, tenue le 12/09) | cinq lots ordonnés (en-tête et onglets ; filtre ; lignes de rôle ; semaines ; lisibilité), tests rouges d'abord, **un checkpoint diff unique** avec tables de hunks par lot ; le dernier lot reste hors index jusqu'au go (détachable en une commande) ; commit unique | choix de Yohan ; lots 1-4 dans l'index, lot 5 dans l'arbre de travail — PR #31 |
| **C8.2** | **Pastille de version** (12/09/2026) | visible seulement hors de l'état stable « Publiée (vN) » — « Modifié, non enregistré », « Aucune version enregistrée », « Version N · non publiée » restent ; toujours visible dans la copie | seul signal « vous avez modifié sans enregistrer » ; `hidden` doublé d'une règle CSS (`display:inline-block` battrait l'attribut) |
| **C8.3** | **Date des données** (12/09/2026) | dans le sous-titre, à la place de « Généré le … » : « Données Doctolib du jj/mm · présence = … » ; `DATA.meta.genere` reste servi, plus affiché | savoir de quand datent les présences décide de relancer un import |
| **C8.4** | **Sélecteur « Planning individuel » retiré** (11/09/2026) | le clic sur un nom du panneau fait la même chose (C6.12 révisée) | redondant à la première vue en production |
| **C8.5** | **Onglets qui suivent le mois** (11/09/2026, mécanique actée le 12/09) | `nav_urls` calculé par le processeur de contexte (`resolver_match.kwargs["mois"]` ou `?mois=`, validé « AAAA-MM », `reverse`, sans requête, présent même pour un anonyme) ; depuis `/planning/<mois>/`, `/presences/<mois>/`, `/absences/?mois=` ; sinon les URL courantes ; le sous-onglet « Présences » suit de même | coûts figés inchangés (10 / 11) ; tests d'isolement et de navigation adaptés (cinq sites) |
| **C8.6** | **Filtre à plusieurs noms** (12/09/2026) | le clic ajoute, un second clic sur le même nom retire ; un genre à la fois (assistantes ou praticiens) ; « Tout afficher » et `Échap` vident ; titre « … · A, B » ; la sélection ne survit pas au rechargement | geste des cases à cocher, sans touche à connaître |
| **C8.7** | **Pictogrammes des lignes de rôle** (12/09/2026) | les quatre (secrétariat, administratif, sureffectif, absent) en SVG inline, libellé en infobulle et `aria-label` ; le libellé texte disparaît | 44 px rendus aux briques par ligne ; une seule grammaire pour les quatre lignes |
| **C8.8** | **Repli d'une ligne de rôle** (12/09/2026) | un clic sur le pictogramme ; portée : toutes les semaines ; mémorisé par type de ligne dans le navigateur (`localStorage`, clé `planning-assistantes.replis`, `try/catch`), **jamais dans `STATE`** ; rendu « picto + N », toujours cible de dépôt, rouverte pendant un placement ; double-clic sans effet | retirer de la vue ce qu'on ne manipule pas, sans grille en dents de scie |
| **C8.9** | **Repli des semaines et jour actuel** (12/09/2026) | chevron sur la bande ; défaut : semaines passées repliées **seulement quand le jour actuel tombe dans la grille**, mois passé ou à venir tout déplié ; jour actuel marqué (date locale calculée côté client, y compris un jour hors mois de la grille) | les semaines faites s'effacent d'elles-mêmes ; un mois ancien se lit entier |
| **C8.10** | **Sommaire de semaines** (12/09/2026) | dans le panneau, hors du corps repliable (visible panneau replié, numéro seul) ; clic = défiler et déplier ; « tout replier / tout déplier » à côté ; le menu « Plus » reste à six entrées | le geste vit là où sont les semaines |
| **C8.11** | **Bandes collantes** (12/09/2026) | bande de semaine `sticky` sous la barre d'outils ; `overflow:clip` sur la semaine (un `overflow:hidden` neutralise `sticky`) | les compteurs restent visibles pendant qu'on pose |
| **C8.12** | **Glisser assisté** (12/09/2026) | défilement de 8 px par image près des bords, qui se tait dès que la page bouge sans lui (défilement natif de Chrome) ; case surlignée ; flash à la dépose | à vérifier à l'usage : Chrome défile peut-être déjà |
| **C8.13** | **Raccourcis ← →** (12/09/2026, contre la recommandation) | d'une semaine à l'autre, seulement sans rien en main, sans modificateur, focus hors champs, panneau et commentaires | choix de Yohan |
| **C8.14** | **Lisibilité** (12/09/2026, renversée le même jour en recette) | +1 px sur la grille, libellés plus foncés, briques **pleines** (fond = encre de la personne, texte blanc ou noir par luminance calculée ; `palette` de `regles.json` intouchée), « hors quota » en double anneau, pâle au papier ; décision A du diff plan (pâle = proposée par le moteur) → **8-bis (PR #32) : toutes pleines**, distinction proposée / confirmée abandonnée (le drapeau reste dans l'infobulle), pâle au papier conservé ; **cases jour** bordées 1,5 px, coins arrondis (`--r-slot`), 6 px d'air | sur un planning publié sans retouche, toutes les briques portaient le drapeau « proposée » : la grammaire rendait tout pâle ; **critère 5 (la principale sur son poste) reste à juger** |
| **C7.1** | **Historique 2026** (07/09/2026) | **import historique** sur le modèle de la 3-quater : fichier produit hors app depuis les 1 113 entrées « Présence » de Notion (colonnes structurées `Praticien` / `Assistante` / `Secretaire` / `Durée` ; titre en repli pour Sureffectif, Administratif, WK, LS), écran d'admin en deux temps ; versions **publiées, marquées « historique » dans `verifications`, sans revérification** ; page planning en lecture pour ces mois ; présences Doctolib **optionnelles** | janvier → août 2026 ; « Mes jours » et le tableau de bord n'ont pas besoin des présences (C4.8) ; le fichier nominatif reste hors dépôt |
| **C7.2** | **Moment de la brique 7** | **pendant la phase de maquettes de la 6**, avant tout diff plan 6a | Claude Code est libre pendant que Yohan itère dans Claude Design |
| **C7.3** | **Octobre 2026** | entré dans l'app **par la page actuelle, sans code** : fichier au format export (`mois`, `numero` 0, quatre clés, briques `a: false`, toute la plage couverte, jours fermés vides) → « Importer… » (fusion jour par jour) → écarts aux règles corrigés dans la page → **v1 publiée le 07/09** ; jours 29/09 → 04/10 repris de la v9 de septembre, Notion listé en écart | Notion portait 146 entrées pour septembre et **156 pour octobre, saisies du 17 au 30/07** ; la v9 de septembre (page, 25/08 → 07/09) fait foi sur Notion pour septembre |
| **C7.4** | **Praticiens partis** | fiches praticien **inactives**, sans agenda, pour Seror (LS, juin) et Krameisen (WK, février → juillet), à créer avant l'import historique | leurs 26 briques ont alors une colonne ; codes à lire dans l'admin, jamais devinés |
| **C7.5** | **Jelly avant septembre 2026** | ses briques « Présence » de Notion antérieures à septembre 2026 (65 entrées depuis le 16/06) sont **ignorées** à l'import historique : elle n'exerce qu'à partir du 08/09 ; « Kelly » (5 absences juin–juillet) est une autre personne, sans fiche, écartée | les deux « absences » de Jelly des 02–03/09 vues le 25/08 n'ont jamais existé |
| **C7.6** | **Régime transitoire jusqu'à la bascule** (07/09/2026) | **principale = Amandine, invitation différée à la finalisation de l'app** (fin de brique 6) ; Notion reste son outil de travail ; l'app porte **une version publiée de chaque mois** (import initial par `cabinet`) ; **en fin de mois**, ré-import du planning vécu (page) et **delta des absences** (écran de la 3-quater, idempotent) → nouvelle version publiée ; à la bascule : mois en attente importés, delta, invitations (Amandine, puis les salariées ; tir de test vers Yohan d'abord) | publier ne déclenche rien tant que la 5 n'existe pas et n'est vu de personne tant qu'aucune salariée n'est invitée ; rendez-vous : fin octobre 2026 |
| **C7.7** | **Mois déjà versé à l'import historique** (08/09/2026, D1) | (1) le mois porte une version **non historique** (sur **toutes** ses versions, pas seulement la dernière) → **refus** ; (2) le mois n'a que des versions historiques et l'**empreinte** du fichier est celle de la dernière → « déjà présente », zéro écriture ; (3) versions historiques seulement et empreinte différente → **nouvelle version historique** (numéro suivant, `version_de_base` = numéro courant) | l'empreinte porte sur `(mois, state)` **canonique**, pas sur les octets : un ré-export du même planning (autre `exporte`) est reconnu ; la voie (3) est la seule correction possible, `PlanningVersion` étant en lecture seule dans l'admin |
| **C7.8** | **Forme d'une version historique** (D2-a) | publiée (`publiee`, `publie_le`, `publie_par` = compte importateur), `verifications` = `{"historique": true, "importe_le", "empreinte", "imports": [], "nb_briques", "nb_jours"}` **sans `verifie_le`** ; **sans revérification** des règles, **sans webhook**, sans passage par `services.publier` ; `services.est_historique` la reconnaît (gardé contre le `[]` des versions non publiées) | grep du 08/09 : aucun lecteur de `verifie_le` hors `test_publication.py` ; « Mes jours » fonctionne tel quel (libellé d'un praticien inactif conservé) ; aucun marqueur d'effectif tant que les présences ne sont pas importées (C6.9) |
| **C7.9** | **Audit de l'import historique** (D4) | **un seul** événement `planning_historique_importe` `{mois, numero, nb_briques, nb_jours, empreinte}` par confirmation ; logs aux mêmes comptages | jamais de nom : les noms n'apparaissent qu'à l'écran d'analyse, pour `cabinet` ; exception `ActionImpossible` locale à `planning/historique.py` (D5) ; `_perime` / `_entier` recopiés depuis `absences/admin.py`, factorisation au backlog (D10) |
| **C7.10** | **Découpage 7a / 7b** (D8) | **7a** = écran d'admin en deux temps sur `PlanningVersion` + « Mes jours » ; **7b** = page de lecture dédiée `/planning/<mois>/historique/` (`sans_import.html` y renvoie, D3), `copie` → **404** (D6), tri fidèle à `construire` (D9) ; **aucune migration** dans les deux (D7). **Contrainte portée au diff plan 6a** (pas à 7b) : un mois dont la version publiée est historique n'est pas « Données Doctolib manquantes » (`not mois_couvert and not est_historique(version_publiee)`) et n'affiche **aucun** marqueur d'effectif (`verifications["imports"] == []`), jamais un 0 | 7a se recette seule (import + « Mes jours ») ; 7b après le merge de 7a, avant tout diff plan 6a (C7.2) ; la page 7b sera rhabillée en 6d comme les autres |
| **C7.11** | **Journées de la page historique** (08/09 et 09/09/2026) | une journée de la plage du mois fait une ligne si le cabinet **ouvre** ce jour-là selon `periodes_ouverture` de `regles.json` (`regles.chargeur.jours_ouverture`, régime daté) **ou** si elle porte au moins une brique ; un jour hors ouverture porteur (dimanche, lundi d'avant le 5/10/2026) est marqué « hors jours d'ouverture » ; un **férié tombant un jour d'ouverture reste une ligne**, vide, marquée « férié » (comme la page normale et « Mes jours » le montrent) ; fériés = calendrier du socle (`socle.feries.feries_entre`) + `state["feries"]` − `state["feries_off"]`, combinaison de `moteur.js::isFerie` | **étend C3.2** : les jours d'ouverture datés servent à la paie **et**, en lecture, à la page historique ; `_doc` de `periodes_ouverture` amendé en ce sens (PR #24) ; un mois vivant reste dessiné d'après les présences Doctolib, sans dimanche ni férié (jour ouvert exceptionnel : § 12) ; la colle « nom du jour ∈ `jours_ouverture` » existe en deux exemplaires (`absences/calcul.py::_ouvert`, `planning/views.py`) → `est_ouvert` public au backlog |
| — | Notion | Notion dit *qui* **jusqu'à la 2**, porte les absences **jusqu'à la 3** ; ~~désormais archive en lecture seule ; les entrées « Présence » ne sont pas migrées~~ — **07/09/2026** : reste l'outil de travail de la principale jusqu'à la bascule (C7.6), ses entrées « Présence » sont reprises (C7.1, C7.3) ; `regles.json` dit *comment* | aucune écriture Notion, jamais |
| — | « Entre autres » | backlog, périmètre v1 = planning | un module portant des données de santé de patients ne s'ajoute pas à cette app sans revoir l'hébergement (§ 5.6) |

---

## 3. Architecture cible

### 3.1 Composants
```
navigateur (PC cabinet, téléphones)          Railway (projet planning-assistante, dédié)
┌─────────────────────────────┐    ┌──────────────────────────────┐    ┌──────────────────────┐
│ page planning (JS validé)   │◄──►│ Django : pages, API JSON,    │◄──►│ PostgreSQL (base de   │
│ espace salariée / admin     │    │ admin, auth, audit           │    │ l'app, dédiée)        │
└─────────────────────────────┘    │ tâche de fond : import S7    │    └──────────────────────┘
                                   └───────┬──────────────▲───────┘
                                           │ POST /admin/presences (secret)   HTTP + secret / webhooks
                                           ▼                                  │
                                   serveur MCP Doctolib (VoiceDoctolib)   n8n Cloud ──► Gmail
                                   lecture seule, zéro PHI                (comptable, salariées, connexion)
```
**En place depuis 1a** : projet Railway **planning-assistante**, service `planning-assistantes` + `Postgres` dédié, région europe-west4, domaine `planning-assistantes-production.up.railway.app`. Webhook n8n « Planning assistantes - Mail sortant » (id `QdyH1ZtTwoDl6mVg`).
**En place depuis 1b** : apps `presences` et `n8n` ; workflows « Import (réception) » (`jb4iRxGeW5XhuaeD`) et « Déclencher import » (`Z4BOQRRxm9KLrQNb`).
**En place depuis 2** : apps `personnes` et `regles`, `comptes/noms.py`, `socle.CompteurDebit`, relevé « topologie proxy ».
**En place depuis 4b** : `planning/conflits.py`, `planning/webhooks.py`, migration `planning.0002`, gabarit `mes_jours.html`, ligne « hors présence » dans `page.js` / `moteur.js` ; **825 tests Python, 57 Node**. Post-squash `0a53bdf`.
**En place depuis 4a** : app `planning` (`models`, `donnees`, `verification`, `services`, `views`, admin lecture seule), statiques `moteur.js` / `page.js` / `styles.css`, tests Node (`node --test`, sans npm, pont pytest), `presences.services.imports_par_date`, `regles.json` section `palette`, blocs `style_base` / `en_tete_page` / `navigation_page` / `classe_html` / `scripts` dans `base.html`. Post-squash `5072226`.
**En place depuis 3** : app `absences` (modèles, calcul pur, services, paie, webhooks, admin, deux commandes de gestion), `socle/feries.py` (fériés français, Pâques incluse), `socle/client_n8n.py` (client factorisé), `comptes/profil.py` (changement d'adresse) ; workflow n8n « Planning assistantes – Absences (réception) » (Webhook `/webhook/absence-planning` en Header Auth → Gmail vers Yohan ; id `eQABsWqayUIR8KuY`, export `docs/n8n/n8n_planning_absences_reception.json`) ; workflow de test « Paie (test) » (Manual Trigger → HTTP GET → Gmail, dupliqué de « Déclencher import »). Aucun de ces workflows n'est « Available in MCP ».

### 3.2 Flux
1. **Import des présences** — en place (1b), chemin fichier ; chemin endpoint câblé, inactif jusqu'à la brique 0.
2. **Page planning — en place (4a, publication 4b)** : `GET /planning/<AAAA-MM>/` sert `DATA` (calculé) et le `STATE` de la dernière version ; la page vérifie les règles strictes avant d'envoyer, le serveur les revérifie (422), refuse une base périmée (409, contrainte unique + `IntegrityError` hors du `with`), enregistre une version (audit `planning_enregistre`) ; copie HTML autonome depuis la dernière version ; erreurs de page remontées (`error.name` seul). **Publier (4b)** : `POST …/versions/<n>/publier/` sur la dernière version, revérification serveur (422), 409 si périmée, audit `planning_publie`, webhook `planning.publie` ; l'en-tête dit « Version N · publiée » ou « publiée : vP » ; « Mes jours » lit la version publiée courante.
3. **Absences — en place (3)** : la salariée (ou la principale, 3-bis) saisit type, dates, précision courte → type *demandé* : `en_attente`, `principale` ou `cabinet` décide (règle K), webhook `absence.demandee` puis `absence.decidee` ; type *déclaré* : `declaree`, effective immédiatement, webhook `absence.declaree` ; à l'entrée en état effectif, jours comptés calculés et **dates retenues figées**, échéance de purge posée si la rétention est réglée ; correction manuelle possible (multiple de 0,5, bornée par la plage), la valeur calculée conservée ; annulation d'une demande en attente, auditée sans webhook. **Conflit avec un planning publié (4b)** : à l'entrée en état effectif d'une absence bloquante, audit `absence_conflit_publication`, webhook `absence.conflit`, bandeau et marqueur sur `/absences/` ; l'absence est écrite dans tous les cas (C4.7).
4. **Paie — côté app en place (3, amendée C5.1)** : la brique 5 ajoute à l'endpoint la catégorie de paie par absence et fait du mail un envoi de **dates + catégorie** ; le paragraphe « N jour(s) » reste indicatif. Tel quel en 3 : `GET /api/n8n/paie/<AAAA-MM>/` rend, par salariée ayant au moins une absence effective d'un type `paie` **dont une portion tombe dans le mois calendaire**, la portion du mois, le total de l'absence, les drapeaux `facture_partiellement` / `corrigee` / `repartition_calculee`, et le paragraphe mis en forme (« Paie 2026-09 — absences à décompter : NOM Prénom : 2,5 jour(s). », avec une phrase d'alerte si une répartition n'a pu être calculée). Une salariée sans absence comptée n'apparaît pas. **Le mail à la comptable reste la brique 5.**
5. **Connexion (1a, en place)** : lien magique django-sesame, 15 min, usage unique.

### 3.3 Contrat `DATA` / `STATE` — ce qui change par rapport à la page actuelle
**Réel depuis 4a.** `DATA` = contrat du brief § 5.2 conservé, plus `meta.imports[{id, empreinte}]` (4b, les imports retenus pour la plage), `meta.non_couverts[]` (dates sans import), `meta.alertes[]` (personne exclue, agenda sans ligne, code en collision, seuils par défaut), `attentes[]` (demandes `en_attente`, informatives). `conges[]` = absences **effectives** de toute nature, une entrée par jour, `bloque = type.bloquant`, `type = libelle` (C4.1) ; une absence `Ecole` d'une étudiante alimente `cours` seulement ; les absences de personnes hors périmètre sont écartées sans alerte. `STATE` = `affectations`, `feries`, `feries_off`, `notes` — rien d'autre (nettoyé côté serveur, `notes` normalisées en liste). Identifiant = `Personne.code`, repli déterministe `code_pour + pk` avec alerte. Export JSON : `mois`, `numero`, les quatre clés, `exporte`. Une absence corrigée à 0,5 reste un jour bloqué (C3.3).

---

## 4. Modèle de données *(figé aux diff plans 1a, 1b et 3 — noms définitifs ; `PlanningVersion` reste indicative)*

| Table | Champs | Notes |
|---|---|---|
| **Personne** *(1a, `comptes.Personne`)* | id, `code` (unique, nullable), nom, prenom, `role_metier`, `heures_hebdo`, `jours_fixes` (JSON), `planifiee`, couleur, `agenda_doctolib`, `email_contact`, actif, `cree_le`, `modifie_le` | jamais : NSS, naissance, téléphone, IBAN, adresse. Contrainte d'unicité (nom, prénom). `modifie_le` n'avance pas lors d'un upsert d'import (`update_fields` sans `auto_now`) — constaté en Phase 1 de la 3 |
| **Compte** *(1a, AUTH_USER_MODEL)* | email (unique), `personne` (OneToOne, PROTECT, nullable), `role`, is_active, is_staff, is_superuser, `invite_le`, `active_le`, `date_creation`, last_login, **`email_en_attente`** *(3)* | mot de passe toujours inutilisable ; `email_en_attente` porte la nouvelle adresse le temps de sa confirmation et rend le jeton à usage unique (§ 5.1) |
| **TypeAbsence** *(3, `absences.TypeAbsence`)* | `libelle` (unique), `bloquant`, `categorie` (`demande` / `declare`), `paie`, `actif`, `ordre` | 13 valeurs posées par migration de données idempotente (`get_or_create` sur le libellé, jamais de réécriture), **orthographe du select Notion reproduite telle quelle** (« Congé évenement familial », « Décés », « Ecole ») ; demandés : Congé payé, Congé sans solde ; paie : ces deux-là plus Maladie, Congé enfant malade, Congé grossesse ; bloquants : les huit premiers du brief § 6 ; modifiable par `cabinet` |
| **AbsenceSalariee** *(3, `absences.AbsenceSalariee`)* | `personne` (FK, PROTECT, `limit_choices_to` assistante / secrétaire), `date_debut`, `date_fin` (`CheckConstraint date_fin >= date_debut`), `type` (FK, PROTECT), `statut` (indexé), `precision` (120 car., facultative), `auteur` (SET_NULL), `cree_le`, `decide_par` (SET_NULL), `decide_le`, `jours_comptes_calcules` et `jours_comptes` (`Decimal(4,1)`), **`jours_retenus`** (JSON, dates ISO), `corrige_par` (SET_NULL), `corrige_le`, `a_effacer_le` (nullable) ; index (personne, date_debut) | **deux valeurs de jours comptés** : la calculée n'est jamais touchée par une correction, la retenue n'est jamais touchée par un recalcul si l'absence est corrigée ; **`jours_retenus`** porte les dates figées au calcul, base de la répartition par mois (C3.6). Décision P (personne salariée seulement) tenue dans `services.creer`, `services.decider`, `clean_personne` et `save_model` de l'admin — `limit_choices_to` n'est qu'un filtre de formulaire. Ni suppression douce ni immuabilité : `annulee` en tient lieu ; une suppression depuis l'admin est journalisée (3-ter) |
| **ImportPresences** *(1b)* | inchangé | |
| **VerrouImport** *(1b)* | inchangé | |
| **CompteurDebit** *(2)* | inchangé | |
| **PlanningVersion** *(4a, figée en 4b, `planning.PlanningVersion`)* | `mois` (char 7), `numero`, `state` (JSON, quatre clés), `version_de_base` (`PositiveIntegerField(default=0)`, **non nul depuis `0002`**), `auteur` (SET_NULL), `cree_le`, `verifications` (JSON : `[]` tant que non publiée ; `{verifie_le, imports[{id, empreinte}], nb_briques}` à la publication), `publiee` (défaut faux), `publie_le` (nullable), `publie_par` (SET_NULL) ; contrainte unique `(mois, numero)` | chaque « Enregistrer » = une ligne ; la publication écrit quatre champs sur la ligne existante, jamais une nouvelle ; rien ne s'efface (admin lecture seule) ; plusieurs `publiee=True` possibles par mois, la courante est la dernière par numéro |
| **EvenementAudit** *(1a)* | `quand`, `qui` (SET_NULL), `action`, `type_objet`, `id_objet`, `details` (JSON) | **sans** type d'absence, précision, donnée de santé, donnée patient, adresse. Actions 1a / 1b / 2 : inchangées. **Actions 3** : `absence_demandee`, `absence_declaree`, `absence_decidee`, `absence_annulee`, `absence_corrigee` (calculé et retenu), `absence_purgee`, `paie_consultee` (mois et nombre de salariées, jamais le contenu), `adresse_changee` ; **3-ter** : `absence_supprimee` (personne, statut, dates). **4a** : `planning_enregistre` (mois, numéro, nombre de briques). **4b** : `planning_publie` (mois, numéro, nombre de briques) ; `absence_conflit_publication` (`personne_id`, `mois`, `numero`, `dates` — jamais le type). `details` ne porte que des identifiants, dates, statuts, comptages — la garde `@` ne reconnaît pas « Maladie », le reste se tient à la main et se prouve par `absences/tests/test_confidentialite.py` |
| **Règles** | `regles.json` versionné, chargé et validé au démarrage | **3** : section obligatoire `periodes_ouverture` (première période non datée = origine, suivantes à date strictement croissante, jours canoniques) ; un fichier sans elle refuse de démarrer — pour de la paie, un défaut d'ouverture silencieux serait pire |

Fériés français : `socle/feries.py`, fonction pure (Pâques incluse), désactivables par mois dans `STATE` (brique 4). Un praticien sans agenda Doctolib apparié n'a pas de mécanisme d'absence : ses jours fixes valent présence.

---

## 5. Sécurité et données

### 5.1 Identité *(1a, complétée en 3 et 3-ter)*
- **Lien magique** django-sesame (15 min, usage unique, invalidation au changement d'adresse). Aucun mot de passe, aucun compte partagé.
- **Création protégée, adresse libre** : invitation sans jeton, compte cabinet assuré au pré-déploiement, commande de secours `lien_connexion`.
- **Changement d'adresse par la salariée — livré en 3** : jeton signé `django.core.signing` portant compte et nouvelle adresse (valable une heure), envoyé à la **nouvelle** adresse ; usage unique par `email_en_attente`, vidé à la confirmation ; adresse déjà prise → réponse neutre sans envoi (doctrine de `/connexion/`) ; `Personne.email_contact` suit ; les liens de connexion en circulation sont invalidés à l'effet (`SESAME_INVALIDATE_ON_EMAIL_CHANGE`). Audité `adresse_changee`, sans l'adresse.
- HTTPS et redirection fournis par Railway ; **`SECURE_HSTS_SECONDS = 31536000` depuis 3-ter** (un an ; sous-domaines et preload restent à `False`, `check --deploy` le signale et c'est voulu).
- Canal d'envoi : C2.9 = a.

### 5.2 Droits et minimisation
- Contrôle porté par `role_requis` ; `/connexion/` et l'API n8n limités en débit (2).
- **3** : `salariee` ne voit que ses absences ; `principale` et `cabinet` voient toutes les absences avec leur type (C2.3b) ; `principale` a aussi son propre espace (3-bis) ; `cabinet` n'y a pas accès (403, conforme).
- **4a** : sept surfaces testées sans nom ni type (`test_confidentialite`) : `state` reçu, `PlanningVersion.state`, export JSON (test Node), audit, logs des vues, corps de `/api/erreurs/`, absences hors périmètre ; `DATA.conges[].type` servi à `cabinet` et `principale` seulement (403 / 302). Les violations sont des codes `{code, date, slot, s}`, le message français est composé par la page.
- **4b** : « Mes jours » ne sert jamais `DATA` (les libellés sont calculés côté serveur depuis le `state` et les personnes) ; testé sans nom d'une autre salariée ni libellé de type ; `cabinet` → 403. Corps de `planning.publie` et `absence.conflit`, audit et logs de publication testés sans nom ni type (`test_confidentialite`, `test_publication`, `test_conflits`).
- **Sorties hors de l'app** — exactement une porte les noms : l'endpoint de paie, réservé à n8n, qui transmet **nom, dates, nombre de jours indicatif et, depuis C5.1 (brique 5), la catégorie de paie — jamais la précision**. Les webhooks d'absence portent identifiants, dates, statut, lien — rien d'autre. Vérifié en recette (§ 14).

### 5.3 Audit et journaux *(1a, étendus 1b, 2, 3, 3-ter, 7a, 7b)*
- Journal d'audit : toutes les transitions d'une absence, la correction, la consultation de paie, le changement d'adresse ; **la suppression d'une absence depuis l'admin (3-ter)** — `Personne` et `Compte` l'étaient depuis 2-bis, `AbsenceSalariee` ne l'était pas, six absences fictives ont disparu sans trace en recette.
- **La leçon du `SET_NULL`, vue en vraie grandeur** : la suppression du compte fictif de recette a vidé la colonne « qui » de dix-sept événements en une seconde. Sur une salariée réelle, ce serait la perte de qui a demandé quoi. Règle réaffirmée : **désactiver, jamais supprimer** ; le correctif structurel monte en tête du backlog (§ 12).
- Logs applicatifs : aucune absence nominative, aucun type, aucune précision, aucune adresse, aucun secret — vérifié en recette sur la fenêtre couverte (§ 14, R2 sur la fenêtre manquante). Les lignes ne disent que « absence #N creee (statut …, N jour(s) de plage) », « decidee », « corrigee », « webhook absence transmis (statut 200) », « paie AAAA-MM consultee : N salariee(s) » ; **4b** : « planning AAAA-MM : version N publiee (N briques) », « absence #N en conflit avec N planning(s) publie(s) », « mes jours AAAA-MM : N jour(s) », « webhook planning non configure ». Vérifié en recette 4b sur toute la fenêtre (§ 14). **7a** : événement `planning_historique_importe` `{mois, numero, nb_briques, nb_jours, empreinte}` (C7.9) ; logs aux mêmes comptages, aucun nom — vérifié à la relecture du diff post-merge (§ 14). **7b** : une ligne `planning AAAA-MM : historique vN lu (N jours)` par lecture ; le 404 de `copie` porte « mois historique : pas de copie autonome ».

### 5.4 Rétention
- **Mécanisme livré en 3 (C3.5)** : `a_effacer_le` posé à l'entrée en état effectif si `RETENTION_ABSENCES_JOURS` est réglée ; commande `purger_absences` (pose d'abord les échéances manquantes, puis purge, journalise `absence_purgee` ; réglage absent = ne fait rien et le dit) ; commande `recalculer_jours_comptes` (rejoue le calcul, ne touche jamais une absence corrigée).
- **Durées à fixer avec le conseil du cabinet** — C2.6 (§ 10). Rien ne casse tant qu'elles ne le sont pas.

### 5.5 Secrets *(noms réels)*
- 1a / 1b / 2 : inchangés. **3** : `N8N_ABSENCE_WEBHOOK_URL` (posée le 04/09/2026 ; absente = notification muette, « webhook absence non configure » dans les logs) et `RETENTION_ABSENCES_JOURS` (**absente à ce jour**, volontairement). **4b** : `N8N_PLANNING_WEBHOOK_URL` (**absente**, à poser avec le workflow de la brique 5). Jamais dans le code, jamais dans un chat.

### 5.6 Frontières
- Inchangées. Rappel tenu en 3 : merge = migrations sur la base de production (trois en 3, aucune en 3-bis ni 3-ter) ; premier mail de décision à une salariée réelle : tir de test vers Yohan d'abord — fait en recette sur une personne fictive, les trois webhooks reçus.
- **Exposition n8n** : aucun des trois nouveaux workflows n'est « Available in MCP », vérifié à la création.

---

## 6. Surfaces

> **Brique 6 (en cours — 6a et 6d livrées ; brique 8 livrée)** : la refonte par rôle est décrite dans `PLANNING_ASSISTANTES_INTERFACE_v1_4.md` (C6.1 → C6.10 ici, **C6.11 → C6.20 et sous-briques 6a-bis / 6b-bis dans le dossier, qui prime pour la 6**). **Depuis la 6d (11/09)** : la page planning porte la barre commune (`barre.css` seule), un en-tête sur une ligne (‹ › vers les mois voisins, pastille de version — masquée sur « Publiée (vN) » depuis la 8 ; la pastille de données a laissé place à la date au sous-titre), Annuler · Enregistrer · Publier + menu « Plus » (Proposer, Exporter (JSON), Importer, Télécharger une copie, Imprimer, Refaire la proposition), ~~filtre « Planning individuel »~~ (retiré en 8 : le filtre vit dans le panneau, à plusieurs noms), toast « rien n'a été publié » sur un 422, bandeau « écran large » ≤ 767 px, `page.js` au vouvoiement, alertes « à signaler au cabinet » pour la principale ; plus de navigation de bas de page. **Depuis la 8 et la 8-bis (12/09)** : onglets de gestion sur le mois de la page ; grille — pictogrammes et repli des lignes de rôle (mémorisé par le navigateur), repli des semaines, sommaire, bandes collantes, jour actuel, flèches ← →, glisser assisté, briques pleines, cases jour bordées ; `moteur.js` et les contrats intouchés (`docs/PLANNING.md` § 14). **Depuis la 6a** : `/` = tableau de bord (`principale`, `cabinet`) ou 302 vers « Mes jours » (`salariee`) ; lien magique refusé → 302 `/connexion/?expire=1` (plus de 403) ; `500.html` autonome ; 403 / 404 avec lien vers la page d'arrivée du rôle ; admin habillé (`/admin/` en cinq blocs). Le reste des routes et comportements ci-dessous reste celui en production tant que 6b → 6d ne sont pas livrées. Constaté le 07/09 : l'écran d'import S7 (`/presences/importer/`) accepte un fichier de planning et répond « appel S7 en échec : » vide — message à rendre explicite en 6c.

### 6.0 Présences *(1b)* — inchangé.

### 6.1 Page planning, versions et publication *(4a, 4b, 7b ; `principale` et `cabinet`)*
- `GET /planning/` (mois courant), `GET /planning/<AAAA-MM>/` (page ; écran « aucun import ne couvre ce mois » si aucun import), `GET /planning/<AAAA-MM>/copie/` (HTML autonome de la dernière version, scripts inlinés par `finders.find`, 404 sans version).
- **7b** — `GET /planning/<AAAA-MM>/historique/` : page **serveur** d'un mois dont la version publiée est historique (sinon 404 ; 403 `salariee`), bâtie sur le `state` seul — ni `DATA`, ni `STATE`, ni `moteur.js`, ni `json_script`, aucune lecture d'absence ; colonnes = slots porteurs de briques résolus **sans filtre `actif` / `planifiee`** (« fiche close »), tri D9 ; cellules aux libellés de « Mes jours » ; journées selon C7.11 ; en-tête « Version N, importée le … — non vérifiée » ; notes ; navigation vers `/planning/<mois±1>/`. L'écran « aucun import ne couvre ce mois » porte « le consulter » vers cette page ; `copie` → 404 sur un mois historique (D6).
- `POST /api/planning/<AAAA-MM>/versions/` : `{version_de_base, state}` → 201 `{numero}` ; 409 `{derniere}` ; 422 `{violations}` sans texte ; plafond 1 Mo ; session + CSRF (`{% csrf_token %}` explicite).
- `POST /api/erreurs/` : `{nom, source, ligne, mois}`, `limite_par_ip` puis `role_requis`, 202, log seul.
- « Enregistrer une copie » grisée tant que l'état diffère de la dernière version ; `beforeunload` ; pas de brouillon navigateur (C2.8 appliquée) ; depuis la 8, `localStorage` ne porte que le repli des lignes de rôle (clé `planning-assistantes.replis`), jamais l'état.
- **4b** — `POST /api/planning/<AAAA-MM>/versions/<n>/publier/` : sans corps ; 200 `{numero, publie_le}` ou `{numero, deja_publiee}` ; 409 `{derniere}` ; 422 `{violations}` ; 400 `mois_invalide` / `numero_invalide` ; 405. Bouton « Publier » actif seulement si la page affiche la dernière version sans modification et qu'elle n'est pas déjà publiée ; refus par la page avant l'appel si une règle stricte est enfreinte ; `META.publiee` et `urls.publier` servis au rendu, recalculés après un enregistrement.

### 6.2 Espace salariée (`salariee`, `principale` depuis 3-bis) *(3, complété en 4b)*
- **4b** — `GET /mes-jours/` (mois courant) et `GET /mes-jours/<AAAA-MM>/` : C4.8 ; lien « Mes jours » dans l'accueil des deux rôles.
- `GET /mes-absences/` : ses absences (motif, état, jours comptés), demande ou déclaration, annulation d'une demande en attente.
- `GET/POST /mes-absences/nouvelle/` : type (types actifs, dans l'ordre), dates, précision courte.
- `POST /mes-absences/<id>/annuler/`.
- `GET/POST /mon-profil/` et `GET /mon-profil/confirmer/<jeton>/` : changement d'adresse (§ 5.1).
- Compte sans `Personne` liée : page « votre compte n'est pas encore rattaché à une fiche », aucune saisie, jamais de 500 (décision H).

### 6.3 Administration et décision *(3, 7a)*
- `GET /absences/` (`principale`, `cabinet`) : demandes à décider, puis absences du mois ; **4b** : bandeau « N absence(s) tombent sur un jour du planning publié » et marqueur « conflit : AAAA-MM vN (dates) » sur la ligne, recalculés au rendu (semaines complètes, comme le planning — la paie seule est calendaire) ; signal « contrat incomplet » sur les absences dont le calcul a rendu 0 (décision N).
- `POST /absences/<id>/decider/` (règle K côté serveur), `POST /absences/<id>/corriger/` (multiple de 0,5, ≥ 0, ≤ jours de la plage).
- Admin Django : `TypeAbsence` modifiable ; `AbsenceSalariee` en création, modification et suppression journalisée ; **3-quater** : écran « Importer un fichier » (`/admin/absences/absencesalariee/importer/`, rôle `cabinet`, `role_requis` hors `admin_view`) — fichier JSON ≤ 1 Mo, rapport d'analyse (verdicts `à créer` / `déjà présente` / `erreur`, jours comptés, signal, conflit avec un planning publié), confirmation liée à l'empreinte SHA-256 et rejouée après 15 minutes, écriture tout ou rien, audit `absence_importee` par absence et `import_absences` par confirmation ; les absences ne portent que des personnes assistante / secrétaire (`clean_personne` rend une erreur de champ lisible ; `save_model` est le dernier rempart).
- **7a** : écran **« Importer un planning historique »** sur l'admin `PlanningVersion` (rôle `cabinet` : `role_requis(Compte.Role.CABINET)` **autour de** `admin_view`, donc `is_staff`, CSRF et `never_cache` conservés ; route enregistrée avant `<path:object_id>/` ; `has_*_permission` toujours `False`) — fichier `.json` ≤ 1 Mo au **format export de la page** (`mois`, `numero`, `affectations`, `feries`, `feries_off`, `notes`, `exporte` ; briques `{a, s, t, x}` ; plage = `presences.fenetres.plage_mois`), analyse **sans écriture** (jours porteurs, briques, colonnes, colonnes sur fiches closes **conservées** avec avertissement C7.4, personnes inconnues, doublons, version existante), confirmation liée en session à l'empreinte et à l'horodatage (15 minutes), analyse **rejouée** à la confirmation et refusée si l'empreinte ou le nombre de briques a bougé, écriture atomique avec l'audit (C7.7 → C7.9). Fixture de tests fictive (`test_pra`, `test_ass`, `test_sec`).

### 6.4 Garde-fous de la décision A *(4a, en place)*
1. Règles strictes revérifiées par le serveur (`planning/verification.py`) à chaque enregistrement, mêmes codes que le moteur JS, **un seul jeu de cas** (`cas_verification.json`) lu par les deux suites. La proposition reste dans la page.
2. Moteur JS isolé (`moteur.js`, fabrique `creer(DATA, state)`, aucune fonction n'appelle `commit` / `render` / `toast` / `confirm`), 55 tests Node (`node:test`, sans npm) ; critère de brique = reproduction exacte d'une proposition figée depuis Chrome avec le gabarit d'origine (129 briques, zéro écart).
3. `POST /api/erreurs/` branché sur `window.onerror` et `unhandledrejection`.

### 6.5 API n8n *(1b, complétée en 3)*
- 1b : `GET /api/n8n/sante/`, `POST /api/n8n/imports/`. Inchangés.
- **3** : `GET /api/n8n/paie/<AAAA-MM>/` (`re_path`, slash final) → `{"mois","debut","fin","salariees":[{"personne_id","nom","code","jours_comptes","absences":[{"absence_id","debut","fin","jours_comptes","jours_comptes_absence","facture_partiellement","corrigee","repartition_calculee"}]}],"paragraphe"}` ; 400 `mois_invalide` ; même patron 429 → 503 → 401 → 405 (recetté : quatre 401, POST compris). `facture_partiellement` dit que la portion de ce mois diffère du total — **pas** que l'absence traverse une frontière de mois (renommé depuis `a_cheval` avant le premier merge, aucun consommateur).
- Sortant : `import.termine` / `import.echec` (1b) ; **`absence.demandee`, `absence.declaree`, `absence.decidee` (3)** — trois événements au lieu des deux du § 6.5 v1.3 ; corps `{evenement, absence_id, personne_id, debut, fin, statut, lien, horodatage}` ; l'annulation est auditée sans webhook ; **4b** : `planning.publie` (corps C4.11, URL `N8N_PLANNING_WEBHOOK_URL`) et `absence.conflit` (corps C4.12, URL absence). Workflow « Planning assistantes – Absences (réception) » : id `eQABsWqayUIR8KuY`, Webhook → Gmail vers Yohan, lit `evenement` dynamiquement, export dans `docs/n8n/`.
- Ce que n8n ne peut pas demander : « calcule la proposition » (décision A).

### 6.6 Import des présences *(1b)* — inchangé.

---

## 7. Règles métier
Implémentées et validées, **pas à re-décider** : brief § 5.4, `regles.json`, et désormais :

**Jours comptés (3, `absences/calcul.py`, module pur)** — **indicateur interne depuis C5.1** : la paie réelle compte les CP et les maladies en jours ouvrables (lundi → samedi, fériés exclus) et le sans-solde en jours réellement non travaillés ; la formule ci-dessous ne coïncide qu'avec ce dernier. Formule C3.1, version corrigée en revue de Phase 2 (la v2 déduisait un férié tombant un jour fermé, et comptait un même jour dans `J` et dans `F`). Ordre des branches aligné sur l'avertissement d'import de la 2 : heures dans les gabarits → contrat horaire ; heures hors gabarits → 0 et signal ; pas d'heures mais jours fixes → jours fixes hors fériés ; ni l'un ni l'autre → 0 et signal. Le plafond `max(0, B − F)` s'applique **une fois par semaine, sur la semaine entière**, et ce sont les **premiers** jours ouvrables de la semaine qui sont retenus : `Resultat.dates` porte les dates, `jours == len(dates)`. Cas de référence (régime mardi→samedi, 39 h) : 07→11/04/2026 = 4 ; 26→30/05 = 4 ; 14/05 seul = 0 ; 12→16/05 = 3 ; 27 h, 29/09→03/10 = 3 ; samedi 03/10 = 1, samedi 10/10 = 0.

**Périodes d'ouverture (3, `regles/chargeur.py`)** — C3.2 ; `jours_ouverture(date)` retient la dernière période dont la date de début est passée.

**Paie (3, `absences/paie.py`)** — C3.6 ; `plage_calendaire` (jamais `presences.fenetres.plage_mois`) ; `portions_par_mois` : non corrigée → un jour retenu vaut un jour au mois où il tombe ; corrigée → prorata au demi-jour inférieur, reliquat au mois du premier jour retenu ; corrigée sans jour retenu → tout au mois de `date_debut`, `repartition_calculee = false` et alerte dans le paragraphe.

**Règles strictes du planning (4a, `planning/verification.py` et `moteur.js`)** — treize codes : `hors_plage`, `salariee_inconnue`, `slot_inconnu`, `brique_invalide`, `doublon_jour`, `jour_bloque` (congé bloquant, cours, **tout férié effectif samedi compris**), `jour_non_affiche`, `sans_donnees`, `praticien_absent`, `capacite`, `exclusive_ailleurs`, `exclusif_intrus` (C4.4), `quota_depasse` (port de `reserve` ; seul endroit où un férié ne consomme une brique que du lundi au vendredi). Les règles relatives (binômes, administratif, continuité, équité) restent dans `proposer`. **4b n'ajoute aucun code** ; la revérification à la publication réutilise `verifier` sur `DATA` recalculé.

**Conflit absence ↔ planning publié (4b, `planning/conflits.py`)** — C4.7 ; `mois_candidats(debut, fin)` (mois dont la plage de semaines complètes touche l'absence) et `conflits_dans_state(state, sid, dates)` sont pures ; `conflits(personne, debut, fin)` lit la version publiée de chaque mois candidat et rend `[{mois, numero, dates}]`.

**Lecture d'un payload S7 (1b)** et **lecture de la fiche personnel (2)** — inchangées.

---

## 8. Briques

| # | Brique | Projet | Contenu | Livrable / critère de succès | État |
|---|---|---|---|---|---|
| 0 | Endpoint `POST /admin/presences` | **VoiceDoctolib** | Phase 1 → 5, contrat aligné sur le client de la 1b | 200 avec le payload S7 tel quel ; 401 ; zéro PHI en logs | à faire — **priorité en hausse** : avec des plannings construits deux à six mois à l'avance (C6.10), l'import manuel des présences est le goulot |
| 1a | Socle + connexion | ce projet | | | **✅ 27/08/2026** |
| 1b | Présences + API n8n | ce projet | | | **✅ 31/08/2026** |
| 2 | Personnes et règles | ce projet | | | **✅ 31/08/2026** (2-bis, 2-ter, 2-quater) |
| **3** | **Absences** | ce projet | types et catégories, espace salariée, décision, jours comptés et **répartition par mois**, endpoint de paie, rétention (mécanisme), changement d'adresse, client n8n factorisé, jours fériés ; **pas de migration Notion** (C3.8) | jeu fictif de cas datés vérifié à la main ; workflow demande → décision → salariée exercé en production sur une personne fictive ; paie calendaire, correction à 0,5, quatre 401 ; logs et audit sans nom, type, précision ni `@` | **✅ 03/09/2026** (3-bis 04/09, 3-ter 06/09 — § 14) |
| **4a** | **Planning — page servie, versions** | ce projet | `DATA` serveur, moteur isolé, double vérification, versions 409 / 422, copie, erreurs, suppression du brouillon | proposition de référence reproduite à la brique près ; 409 en production ; refus page sur praticien absent ; logs sans nom ni type | **✅ 06/09/2026** (R4a-1 différé) |
| **4b** | **Planning — publication** | ce projet | publication (C4.5, C4.6, audit, webhook), conflit absence / planning publié (C4.7), « Mes jours » (C4.8), ligne « hors présence » (C4.9), `version_de_base` non nul (C4.10), `meta.imports` | v4 → v8 de septembre publiées en production ; « Mes jours » d'une personne fictive ; conflit signalé par audit, webhook et bandeau ; 422 serveur sur enregistrement ; import de la brique orpheline manipulable ; logs sans nom ni type | **✅ 07/09/2026** (R4b-1) |
| **3-quater** | **Reprise de l'existant 2026** | ce projet | écran d'import admin (C3.9), `services.importer / analyser_import / executer_import`, `FormulaireImport`, deux actions d'audit ; aucune migration | 201 absences importées en production depuis le fichier arbitré sur les bulletins ; ré-import → « déjà présente » partout ; conflits de septembre signalés dans le rapport ; logs sans nom ni type | **✅ 07/09/2026** (PR #19 ; docs PR #20) |
| **6** | **Interface** | ce projet | refonte par rôle (C6.1 → C6.10, dossier v1.4) : **6a coquille ✅** ; **6d enveloppe de la page planning ✅** ; **6a-bis ✅ (réduite, C6.21)** ; **6b** espace salariée téléphone ; **6c** absences et données — ordre C6.18 / C6.20 | maquettes validées ; navigation par rôle testée ; grille au pixel près ; tests verts plus les nouveaux ; `moteur.js`, `verification.py`, `donnees.py`, `services.py` absents du `git diff --stat` ; aucune migration | **6a ✅ 10/09/2026** (PR #26, `2e30150`) ; **6d ✅ 11/09/2026** (PR #29, `a39c3d5`, 1 020 / 57 ; recettée le 12/09 ; docs PR #30 `8db7c50`) ; **6a-bis ✅ 12/09/2026** (PR #35, `4b1dd6f`, 1 045 / 57 ; recette déclarée OK) ; 6b, 6c : à cadrer |
| **7a** | **Historique 2026 — import** | ce projet | écran d'admin en deux temps sur `PlanningVersion` (C7.7 → C7.9), `planning/historique.py`, `services.est_historique`, fiches inactives Seror / Krameisen (C7.4), fichiers janvier → août produits hors app depuis Notion (C7.1, C7.5) | huit mois visibles dans le tableau de bord et « Mes jours » ; ré-import → « déjà présente » ; rapport jour par jour ; aucun nom dans les logs ; aucune migration | **✅ 08/09/2026** (PR #21, `95e6ba9`, 917 / 57 ; docs PR #22 `6961fca` — § 14) |
| **7b** | **Historique 2026 — lecture** | ce projet | page dédiée `/planning/<mois>/historique/` (C7.11), lien depuis `sans_import.html`, `copie` → 404, tri fidèle à `construire` (C7.10) ; fériés du socle, `feries_off` appliqué | mois historique lisible sans import de présences ; `copie` → 404 ; 403 salariée ; `moteur.js`, `donnees.py`, `services.py` intouchés ; aucune migration | **✅ 08/09/2026** (PR #23, `fdfc912`, 939 / 57 ; recette 09/09 ; docs PR #24 `eec1940` — § 14) |
| **8** | **Grille du planning** (lisibilité, filtres, repli, circulation) | ce projet | C8.1 → C8.14 : en-tête allégé et onglets qui suivent le mois (retours 6d), filtre à plusieurs noms, pictogrammes et repli des lignes de rôle, repli des semaines, sommaire, bandes collantes, jour actuel, flèches, glisser assisté, briques pleines ; **8-bis** : toutes pleines, cases jour bordées ; `moteur.js`, `views.py` et les contrats intouchés | proposition inchangée (« Exporter (JSON) » avant / après identique) ; copie hors ligne ; captures 1 536 × 864 après ; onglets sur le mois ; 57 Node ; aucune migration ; « je vois mieux, je clique moins » (principale) | **✅ 12/09/2026** (PR #31, `2522e97`, 1 039 / 57 ; **8-bis** PR #32, `89885de`, CSS seul ; docs PR #33 `8707429` ; recette 12/09 sur émulation — critère 5 avec la principale à suivre — § 14) |
| 5 | Notifications | n8n | mail comptable depuis `GET /api/n8n/paie/` (**dates + catégorie de paie, C5.1**) ; workflow `planning.publie` + `N8N_PLANNING_WEBHOOK_URL` ; tir de test vers Yohan ; mails du backlog si souhaité | ancien workflow désactivé, pas supprimé | à faire |

Jeu de données de la 1b : inchangé. Jeu d'absences : **fictif** exclusivement, en tests comme en recette (personne `TEST Recette`, supprimée après).

---

## 9. Régime de travail (rappel)
WORKING_HABITS s'applique intégralement. **Confirmé en pratique sur 1a, 1b, 2 et 3** : le diff plan exhaustif comme checkpoint unique ; la revue technique de Claude Code contre le code réel entre v1 et v2 du plan (une correction **bloquante** en 3 : la formule de paie) ; **la relecture du checkpoint diff par Claude conversationnel a trouvé en 3 un défaut de paie que 606 tests verts ne voyaient pas** — les tests étaient verts sur un comportement faux, parce que le plan ne disait pas sur quelle plage la paie devait filtrer ; l'assistance interactive à la recette (jeu local, contrôles négatifs, comptage de logs) ; les gestes dans le navigateur, n8n et l'admin à Yohan.

---

## 10. Points restants (implémentation, pas périmètre)

| # | Question | Options | Tranché |
|---|---|---|---|
| C2.6 | **Valeur** des durées de rétention (absences, données ayant servi à la paie, versions, audit) | à fixer avec le conseil du cabinet ; **le mécanisme est livré (C3.5), la valeur se pose dans `RETENTION_ABSENCES_JOURS`** | avant que la première absence réelle atteigne une échéance plausible |
| ~~—~~ | ~~Calcul des jours comptés ; correction par la validatrice~~ | **tranché C3.1 / C3.3, livré en 3** | ✅ 01/09 |

---

## 11. Éléments d'entrée à fournir
- ~~**Étalon de paie**~~ — **acté le 07/09/2026** autrement que prévu : les **bulletins de janvier → août** (rubriques d'absence avec dates, lus par Claude conversationnel, jamais versés) ont servi d'étalon, plus fiable que la sortie du workflow Notion. Résultat : sans-solde identique ; CP et maladies décomptés en **jours ouvrables** par la comptable (Cécile 03 → 29/08 : 24 sur bulletin, 12 par C3.1). D'où C5.1. Le workflow « PROD – Fiche de paie V2 » n'a jamais été retrouvé ; il n'est plus nécessaire.
- ~~**R4a-1 — planning réel de septembre 2026**~~ — **acté le 06/09/2026** : brouillon du 25/08 repris depuis Chrome, 25 jours importés dans `/planning/2026-09/`, **une violation expliquée** (`praticien_absent` le 09/09 : Doctolib avait bougé entre le 25/08 et l'import #6), corrigée par export JSON puis enregistrée en **version 4**. Écart de page **E1** (brique orpheline invisible) → C4.9. Les absences de septembre restent à ressaisir (C3.8) pour que les compteurs redeviennent fidèles.
- **Fournis le 07/09** : export JSON de la v9 de septembre (codes réels des salariées et des slots), captures de la page planning (septembre v9, octobre après import), de l'admin Personnes et de l'écran S7. **Attendus pour la 6** : rapport `brique6_phase1_inventaire.md`, captures de `/absences/`, `/mes-absences/`, `/mes-jours/` (téléphone).
- Fournis le 26/08 : capture de la page « Absence » et du select `Type` ; export du workflow de paie. La formule Notion « Durée » n'a jamais été fournie : la formule C3.1 a été fixée sans elle, l'étalon ci-dessus est le filet.

---

## 12. Backlog (hors v1, sans décision)
- ~~`PlanningVersion.version_de_base` nullable~~ — **fait en 4b (C4.10)**.
- **Issus de la 3-quater** : l'écran d'import n'a plus de source (Notion est archive) — le garder ou le retirer en v2 ; « Absence justifiée » n'a pas été créée (aucune entrée sans type après arbitrage) ; deux Lea dans la fiche → `identifiant()` les distingue par le nom, les libellés courts de la page (« Lea D », « Lea W ») restent à la charge de la page.
- **Repris par la brique 6** : vue « Mes jours » adaptée au téléphone, `DATA` qui vieillit dans un onglet ouvert, toast du 422 de publication, style du badge « hors présence » (voir ci-dessous).
- **Issus du 07/09 (soir)** : samedi de **secrétariat seul** (aucun praticien) — aujourd'hui sans colonne (règle « jours où quelqu'un est présent ou a un jour fixe ») ; si le besoin apparaît : colonne repliée avec bouton « Ouvrir » (préférence Yohan), touche la règle partagée JS / Python → mini-brique à part ; **v10 de septembre** (Adrien sans assistante les 29 et 30/09 — Notion disait Amandine puis Dilsa —, brique de Jelly en semaine 36 avant sa prise de poste) ; date d'entrée par personne (l'app n'en a pas, le moteur planifie dès qu'une fiche est planifiée) ; écran S7 : message explicite quand le fichier n'est pas un payload ; « Importer… » fusionne jour par jour — documenter ou proposer « remplacer ».
- **Issus de la 7b (09/09)** : exposer un **`est_ouvert(date, regles)` public** dans `regles.chargeur`, consommé par `absences/calcul.py::_ouvert` et `planning/views.py::_lignes_historiques` (deux copies d'une même colle aujourd'hui) ; ~~`docs/PLANNING_ASSISTANTES_CADRAGE.md` est en v1.7 dans le dépôt~~ **fait le 10/09 (PR #25, `d0f7bd8`, v1.10 copiée verbatim — même blob `49cfce8` —, citations README / CLAUDE.md)** ; les deux citations disent encore « décisions C2 → C5 » : à corriger à la synchronisation de cette v1.11 ; `reference/skill-v1/regles.json` (copie de référence, non chargée) n'est pas à aligner.
- **Issus de la recette 8 et 8-bis (12/09)** : ~~critère 5 à juger avec la principale sur son Chromebook~~ **atteint le 12/09 (soir), Chromebook, app en direct — pas de 8-ter** ; brique en main, un clic sur un pictogramme replie la ligne au lieu de poser (poser ailleurs sur la ligne fonctionne) — à observer, 8-ter ou phrase de doc ; idée « **publié / non publié** visible dans la grille » (au-delà de la pastille de version) — sans décision ; ~~captures du 12/09 à ranger~~ **fait le 12/09 (soir)** : `captures_8\` (quatre « avant 8 » + `exports\`, copies prouvées par empreinte ; « après 8 » vues en séance, non archivées), doublon de `captures_6d\` supprimé sur preuve, critère « export avant / après identique » **prouvé hors `exporte`** ; cleanup local des quatre branches (égalité d'arbre, `main` rapatrié en `--ff-only`) ; C6.18 disait 1 366–1 536 px : 1 536 seule fait foi ; les gestes JS de la grille ne sont pas testés côté serveur (gardes de source seulement) — assumé.
- **Issus de la recette 6d (11/09, partielle)** : en-tête trop chargé (pastilles « Publiée (v1) », « Données Doctolib du … », sélecteur « Planning individuel » jugés redondants) → brique 8 (C6.20, ✅ 12/09) ; filtre à plusieurs noms et onglets qui suivent le mois → brique 8 (✅) ; ~~**PR docs 6d à faire**~~ **fait le 12/09 (PR #30, `8db7c50`)** (`docs/INTERFACE.md` § 2 : la page planning reçoit la barre par `barre.css` seule, § 8, § 10 ; `CLAUDE.md` : règle d'isolement reformulée ; `README.md`) ; ~~cleanup local de `brique-6d-enveloppe`~~ **fait le 12/09 (treize branches, égalité d'arbre)** ; ~~`avant_2560_ouvert.png` à refaire à 100 %~~ **fait** ; ~~reste à constater :~~ **constaté ou déclaré le 12/09 (§ 14) :** grille au pixel près aux trois largeurs, menu avatar au-dessus de la barre d'outils, « Plus », publication sans rechargement, copie hors ligne, alerte principale, impression, bandeau écran large.
- **Issus de la recette 6a (10/09)** : tableau de bord et index d'admin **trop vides à 2 560 px** (conteneur de 1 440 px, cartes remplies au tiers), « Ajouter » de l'admin trop discret → **6a-bis** ~~« aménagement » (CSS et gabarits, à cadrer pour 1 366–1 536 px **et** 2 560 px)~~ **réduite le 12/09 et livrée (C6.21) ; l'aménagement 2 560 px n'est plus au backlog sauf retour de la principale** ; pas d'accès à l'administration depuis la page planning → 6d (barre haute sur la page) ; ~~tableau de bord : deux requêtes par mois listé … ; `<details>` du menu avatar sans fermeture au clic extérieur~~ **faits en 6a-bis** ; double déconnexion et boutons nus sur les pages 6b / 6c / 6d jusqu'à leur rhabillage ; exports Claude Design du 10/09 sans les corrections 2 et 3 du § 10 du dossier (le dossier prime, rien à régénérer) ; écran d'import des absences non rejoué à la main en recette 6a (suites `test_import.py` vertes).
- **Issus de la 7a (08/09)** : factoriser `_perime` / `_entier` entre `absences/admin.py` et `planning/admin.py` (D10) ; **marqueurs d'effectif sur l'historique** = huit fenêtres S7 à tirer dans VoiceDoctolib puis commande de rattrapage C6.9 (option, jamais décidé) ; l'écran d'import historique n'a plus de source une fois 2026 repris (même sort que l'écran de la 3-quater) ; `docs/PLANNING.md` et `README.md` mis à jour en PR séparée (Phase 6 de la 7a).
- **À cadrer après la brique 6** (dans la version du cadrage qui la clôturera ; la 7b aura sa v1.10 avant) : **brique 5 (n8n) à recadrer** depuis C6.16 (l'envoi de paie à la comptable devient un envoi SMTP de l'app : récapitulatif + justificatifs ; ce que garde le workflow n8n reste à décider) ; brique **« connexion par mot de passe »** (invitation par lien magique une fois, puis mot de passe — touche la posture C2.1 : longueur, verrouillage, réinitialisation, coexistence, audit) ; **jour ouvert exceptionnel** (garde un dimanche ou un férié, aujourd'hui tenus pour fermés par la page — à poser avant qu'une garde réelle se présente) ; C2.6 (valeur de rétention) ; brique 0.
- **Issus de la recette 4b** : le `DATA` d'un onglet ouvert vieillit (une absence saisie après le rendu n'est vue qu'au refus serveur) → rechargement au focus ou « données du … » dans l'en-tête ; toast d'`afficherViolations` sur un 422 de publication (« rien n'a été enregistré ») ; `__str__` d'`EvenementAudit` en UTC alors que « Quand » est en Paris ; style de l'étiquette « hors présence » (badge noir, à aligner sur un libellé italique) ; un jour hors colonnes portant une brique orpheline (export JSON seul).
- **Conservation structurelle de l'auteur d'un événement d'audit après suppression de son compte** — **monte en tête** : vu en vraie grandeur en recette 3 (§ 5.3). Tant que ce n'est pas fait, la règle « désactiver, jamais supprimer » est la seule protection.
- Mails : demande → validatrice ; décision → salariée ; publication → chaque salariée ; conflit après publication → principale ; correctif de paie après le 20. Les webhooks `absence.*` de la 3 sont les déclencheurs prêts.
- **Annulation d'une demande (S2e)** : non exercée en production, couverte par les tests — à exercer à la première occasion réelle.
- **Saisie native en demi-journées** (champs matin / après-midi) si la correction manuelle devient trop fréquente ; changerait le modèle et le planning.
- **Export de logs Railway** : plafonné, et un redéploiement coupe la fenêtre — exporter par déploiement ; un connecteur Railway en lecture pendant la recette éviterait la manipulation (à instruire).
- Hygiène post-recette 2 : inchangée (pluriel du rapport d'import, avertissement « absente du fichier »).
- Types « paie » supplémentaires : drapeau à cocher par `cabinet`, aucune décision.
- Le « entre autres » ; exposition de l'app à Claude via MCP ; vue « Mes jours » adaptée au téléphone (la page 4b est minimale, non stylée) ; passage de la logique côté serveur (option B) ; hygiène du conftest racine (partiellement réglée en 3 : fixtures remontées, bloc `os.environ` toujours inopérant) ; performance de `couverture()` ; verrou d'import non supprimable ; point d'état par lot ; second tir du canari ; taux de remplissage ; doctrine des couleurs — inchangés.
- ~~HSTS à un an~~ — **fait en 3-ter**.
- ~~Limitation de débit~~ — livrée en 2. ~~Factorisation du client n8n~~ — **faite en 3 (C3.7)**.
- **Hors projet, pour VoiceDoctolib** : le serveur MCP journalise en clair l'adresse du compte technique Doctolib (« Email saisi », « Polling IMAP … ») — 18 occurrences dans 999 lignes, constaté en cherchant un export de logs pendant la recette 3.

---

## 13. Glossaire
Inchangé depuis la v1.3, plus :
- **Jours comptés** : ce que la paie décompte pour une absence ; **calculés** (par la formule C3.1, jamais modifiés) et **retenus** (la valeur envoyée, corrigeable). **Jours retenus** : les dates que la formule a réellement comptées, figées au calcul ; base de la répartition par mois.
- **Portion** : la part des jours retenus d'une absence qui tombe dans un mois calendaire donné.
- **Période d'ouverture** : les jours de la semaine où le cabinet ouvre, à partir d'une date ; sert au calcul de paie, pas au planning.
- **Contrat incomplet** : personne sans heures ni jours fixes, ou aux heures hors gabarits ; le calcul rend 0 et un signal, la validatrice saisit.
- **Recouper** (interdit) : découper une absence par mois pour relancer le calcul sur chaque morceau — le plafond hebdomadaire s'appliquerait deux fois.

---

## 14. État des briques et journal de livraison

### Briques 1a, 1b, 2 — inchangées (voir v1.3).
Complément 2 : la **désignation de la principale** dans l'admin et l'**envoi réel des invitations** aux salariées restent des gestes de Yohan. **07/09/2026** : principale = Amandine, désignation et invitation **différées à la fin de la brique 6** (C7.6).

### Brique 6a-bis — Aménagement (réduite) — ✅ livrée le 12/09/2026 (soir), recette déclarée OK
- Branche `brique-6a-bis-amenagement` (depuis `main` = `4b828c8`), **PR #35**, hash post-squash **`4b1dd6f`** (commit de branche `7ed7a1e`), branche distante supprimée ; sept fichiers, +127 / −16 : `_bloc_admin.html`, `administration.css`, `base.html`, `socle/tableau_de_bord.py`, trois fichiers de tests ; **1 045 tests Python** (+6) et **57 Node** ; aucune migration.
- **Préalables du 12/09 (soir)** : critère 5 de la 8 atteint (principale, Chromebook, app en direct) ; cleanup local des quatre branches 8 / 8-bis / docs-8 / cadrage (preuve d'égalité d'arbre, `main` local rapatrié en `--ff-only` : la PR #34 était mergée, jamais tirée) ; `captures_8\` (copies prouvées par empreinte, exports avant / après identiques hors `exporte`) ; doublon de `captures_6d\` supprimé ; dossier interface v1.5.
- **Démarche** : périmètre **réduit par Yohan** avant toute fiche (trois points, sans aménagement 2 560 px) → Phase 1 lecture seule (rapport de 319 lignes, copies numérotées ; mesure du coût de `/` par un test temporaire supprimé : 8 + 2 × L ; quinze questions) → décisions point par point (D6a-bis.1 → .3) → diff plan v1 (décisions A → E, sept questions) → **revue technique contre le code** (7 écarts, 2 bloquants : un compte de `<script` inatteignable, `couverts` en `date` au lieu de chaînes ISO ; E-2 prouvée par `lecture.py:185-193`) → v2 vérifiée par empreinte → Phase 3 en trois lots, tests rouges d'abord (1, 3, 4 rouges) → checkpoint diff unique (trois tables de hunks, zéro écart de ligne, quatre nuances acceptées) → go → PR #35 → deploy.
- **Recette** : logs Railway propres (aucune migration, `pre_deploiement termine`, aucun nom) ; `/admin/`, `/` et `/planning/2026-10/` **déclarés OK par Yohan sans capture**.
- **Phase 6** : cette v1.14 ; dossier interface v1.6 ; docs du dépôt (PR à part).

### Brique 8 — Grille du planning — ✅ livrée le 12/09/2026 (8-bis le même jour), recettée le 12/09 sur émulation, **critère 5 atteint le 12/09 (soir)** — docs PR #33
- Branche `brique-8-grille` (depuis `main` = `8db7c50`), **PR #31**, hash post-squash **`2522e97`** (commit de branche `b8ce4db`) ; neuf fichiers, +365 / −87 : `page.js`, `styles.css`, `_corps.html` (−2), `socle/contexte.py` (`_mois_de`, `_nav_urls`), `base.html`, quatre fichiers de tests ; **1 039 tests Python** (+19 : cinq gardes de source dans `test_enveloppe.py`, un paramétré dans `test_navigation.py`, onze cas dans `test_contexte.py`) et **57 Node** ; aucune migration. **8-bis** : branche `brique-8-bis-cases`, **PR #32**, **`89885de`** (commit `fafac14`), `styles.css` seul (+7 / −10). **Docs** : PR #33, **`8707429`** (`docs/PLANNING.md` § 14 nouvelle, § 4 et § 10 corrigés ; `docs/INTERFACE.md` § 2, § 3, § 9, § 10 ; `README.md` ; `CLAUDE.md`).
- **Préalables du 12/09 (matin)** : recette 6d close ; docs 6d + cadrage v1.12 (PR #30, `8db7c50`) ; cleanup local — treize branches supprimées après preuve que l'arbre de chaque pointe est porté par un commit de `main` (deux temps : lecture, puis `-D` sur go), `fetch --prune`.
- **Démarche** : fiche v0.1 → **quatorze décisions une à une** (D8.1 → D8.14 ; deux ajouts de Yohan : jour actuel, flèches ; D8.14 « échelle et contraste maintenant » contre la recommandation) → fiche v0.2 → **Phase 1 lecture seule** (rapport de 2 324 lignes : `page.js` 544 lignes copié numéroté hors dépôt, sélecteurs de `styles.css`, gabarits, contexte, tests lisant la page, structure de `palette`, mesures System.Drawing à DPR 2 — chrome 107,5 px, colonne de jour 256 / 293,5, bande 68,5, ligne de rôle 28, brique 21) → **diff plan v1** (cinq lots, décisions A → E, tables de hunks sur les lignes de `8db7c50`) → **revue technique contre le code** par Claude Code : six corrections, dont deux comportements faux (`hidden` battu par `display:inline-block` ; liseré « hors quota » invisible sur une encre sombre), cinq sites de test oubliés, `VIDE` de `test_contexte.py`, sommaire à 28 px ; questions Q1 → Q4 tranchées → **v2** (282 lignes), contrat vérifié par empreinte SHA-256 au lancement → Phase 3 : lot par lot, **tests rouges d'abord** (onze au lot 1), lots 1-4 dans l'index, lot 5 dans l'arbre de travail, suite complète, garde `.env` (`DATABASE_URL` absente → SQLite), `runserver` sans captures (connexion par lien magique) → **checkpoint diff unique** (cinq tables, trois diffs) relu ligne à ligne → deux corrections d'une ligne (flash au clic, pointillé de journée courte au papier) → « go avec le lot 5 » → merge.
- **Recette (12/09, émulation 1 536 × 864, octobre publié)** : constaté sur captures — sous-titre daté, en-tête sans pastille de données ni sélecteur, pastille de version masquée sur « Publiée (v1) », barre d'outils sur une ligne, pictogrammes, chevrons, sommaire et « tout replier / déplier » (panneau ouvert et replié), tout déplié et aucune colonne marquée (le 12/09 n'est pas dans la grille) ; déclarés OK sans capture — filtre à deux noms, repli mémorisé, bandes collantes, flèches, glisser, copie hors ligne, impression, export JSON avant / après. **Constat** : toutes les briques pâles — un planning publié sans retouche garde le drapeau « proposée » → la distinction ne sert à personne → **8-bis** en une PR CSS (toutes pleines, cases jour bordées 1,5 px, `--r-slot`, 6 px d'air), déclarée OK par Yohan. Écart noté : brique en main, un clic sur un pictogramme replie la ligne au lieu de poser.
- ~~**Reste ouvert** : critère 5 avec la principale sur son Chromebook ; captures du 12/09 à ranger~~ **soldés le 12/09 (soir)** : critère 5 atteint (Chromebook, app en direct), captures rangées ; reste l'idée « publié / non publié » (§ 12). **Brique 8 close.**

### Brique 6d — Enveloppe du planning — ✅ livrée le 11/09/2026, recettée le 12/09 — docs PR #30
- Branche `brique-6d-enveloppe` (depuis `main` = `c31f0a9`), **PR #29**, hash post-squash **`a39c3d5`** (commit de branche `b60455f`), branche distante supprimée (locale nettoyée le 12/09). 13 fichiers, +456 / −107 : `page.js` (63), `styles.css` (51, quatre hunks, tous colonne enveloppe), `_corps.html`, `page.html`, `views.py` (72), `base.html` (bloc imbriqué `titre_page`), `commun.css` (−27 vers `barre.css`), **`socle/static/socle/barre.css` nouveau** (39 lignes, autonome), tests ; **1 020 tests Python** (+8 `planning/tests/test_enveloppe.py`, +1 `socle/tests/test_contexte.py`) et **57 Node** ; aucune migration.
- **Contexte** : PR #27 (`266d8f3`, docs 6a : `docs/INTERFACE.md` nouveau, `PLANNING.md`, `README.md`, `CLAUDE.md`) et PR #28 (`c31f0a9`, cadrage v1.11 verbatim, citations « v1.11 — 10/09/2026, C2 → C7 ») le matin même.
- **Démarche** : pas de Phase 1 nouvelle (rapport de la 6 du 08/09) ; **captures d'émulation Chrome** (1 366 × 768, 1 536 × 864, ouvert / replié) fournies par Yohan et **mesurées** par Claude Code (barre d'outils 100 px, boutons sur une ligne finissant à 1 342 px, colonne de jour 222 / 260 / 256 / 294 px) → **diff plan v1** (668 lignes : § 0.2 inventaire de `styles.css` en deux colonnes enveloppe / grille, § 0.6 tests lisant la page planning, a1 rejetée pour trois règles de `commun.css` qui toucheraient la grille) → relecture : **`barre.css` autonome** (la barre s'appuyait sur `button` nu, `a`, `html, body` de `commun.css`) → **décisions A → K** (a2 `barre.css` ; b1 date locale ; c1 pastille seule ; D 767 px bandeau seul ; E `<select>` ; F `<details>` ; g3 « à signaler au cabinet » ; H quatorze chaînes ; i1 ; J une ligne, m2 ‹ › avec URL de la vue, s1 sous-titre 11 px ; K libellés du tableau de bord) → **v2** (553 lignes ; revue technique 19 lignes, 6 corrections, 0 bloquant : `body.planning nav…` 307-310 atteignait la barre, `h1` générique → `.topbar h1`, `importe_le` aware → date locale sans `Date`, `.plus-menu` `:hover`) → Phase 3 : **tests rouges d'abord** (13, rien hors liste) → 1 020 verts → **checkpoint diff avec tables de hunks** (`styles.css` 4 hunks, `page.js` 19 remplacements dans les fonctions autorisées ; 6 écarts nommés, acceptés ; `_corps.html` sans `{% url %}` ni `href="/`) → deux vérifications (identifiant du filtre = chaîne ; `--font` défini des deux côtés) → merge.
- **Recette (11/09, partielle)** : en-tête vu par Yohan → **pastilles et sélecteur jugés redondants** (C6.20, brique 8) ; **non encore constaté** : grille au pixel près aux trois largeurs, menu avatar au-dessus de la barre d'outils, « Plus », publication et « Publiée (vN) » sans rechargement, copie hors ligne, alerte principale, impression, bandeau écran large. Deux demandes supplémentaires nées de la recette : filtre à plusieurs noms (un genre à la fois), onglets qui suivent le mois — brique 8.
- **Recette close le 12/09** : constaté sur captures — grille et en-tête à 1 536 × 864 (panneau ouvert / replié) et à 2 560 px à 100 %, menu « Plus », impression ; déclarés OK par Yohan sans capture — menu avatar au-dessus de la barre d'outils, copie hors ligne, publication sans rechargement, bandeau écran large ; non constatable — alerte « à signaler au cabinet » (aucun compte `principale` en recette, C7.6) ; logs non relus ; à 1 366 px la barre d'outils passait sur deux lignes — hors critère (poste 1 536), résorbé par la 8.
- ~~**Reste ouvert** : PR docs 6d ; cleanup local ; fin de recette ; captures Chromebook réelles ; brique 8 (fiche v0.1, nouvelle session).~~ **Tout soldé le 12/09** (PR #30 `8db7c50` ; treize branches supprimées après preuve d'égalité d'arbre ; recette close ; brique 8 livrée) — les captures du poste réel passent au critère 5 de la 8.

### Brique 6a — Coquille — ✅ livrée le 10/09/2026, recettée le même jour
- Branche `brique-6a-coquille` (depuis `main` = `d0f7bd8`), **PR #26**, hash post-squash **`2e30150`** (commit de branche `e0173ff`), branche supprimée après vérification de l'égalité des arbres. 32 fichiers, +1 272 / −191 : 12 modifiés, 19 nouveaux, 1 supprimé (`socle/templates/socle/accueil.html`, D-3) ; **1 011 tests Python** (+74 : `socle/tests/test_contexte.py` 24, `test_navigation.py` 21, `test_tableau_de_bord.py` 15, `test_erreurs.py` 11, `test_admin_habillage.py` 3 ; −2 : `test_base_inchangee…`) et **57 Node** ; aucune migration ; statiques nouveaux `socle/static/socle/{polices,commun,administration}.css` et `favicon.png`.
- **Contexte** : le matin, **PR #25** (`d0f7bd8`) avait porté la copie du cadrage v1.10 dans le dépôt (verbatim, blob `49cfce8` vérifié, aucune ligne perdue) et corrigé les citations de `README.md` et `CLAUDE.md` ; cleanup local : après un squash-merge, `git branch -d` refuse toujours — `-D` n'est autorisé qu'après égalité des arbres (leçon § 15).
- **Démarche** : Phase 1 de la brique 6 (rapport du 08/09) → dossier v1.2 → maquettes validées le 10/09 → **diff plan v1** (965 lignes, hors dépôt) avec un § 0 de faits complétés fichier:ligne (libellés réels de l'admin, ordre de recherche des gabarits par `INSTALLED_APPS`, `login_failed` de sesame, blocs de `base.html` vidés par `page.html`, sources du tableau de bord, tests lisant l'accueil) → relecture Claude conversationnel : **trois corrections** (garde anonyme et paresse du processeur de contexte, `app_name` de `socle` à vérifier, salutation en double) → **décisions A → I** de Yohan par questions à choix (C6.17) → **v2** (858 lignes) avec **revue technique contre le code et Django 5.2.17 : 18 écarts, 1 bloquant** (le processeur de contexte lisait `user.personne` sans garde → 500 sur la page de connexion) → Phase 3 : **tests rouges d'abord** (37 rouges et 2 modules manquants, rien hors liste) → implémentation → 1 011 verts, `check`, `makemigrations --check`, `collectstatic` → **checkpoint diff : 9 écarts nommés, tous acceptés, une retouche** (la pilule bleue réservée à `.bouton` : « Valider » et « Refuser » de `/absences/` seraient devenus deux boutons identiques) → merge → recette.
- **Recette (10/09, production, cabinet, captures)** : page planning **au pixel près** (9 / 4 / 3 requêtes figées par test, identiques à `main`) ; tableau de bord conforme — « importées jusqu'au 1 novembre », octobre publié **sans** pastille « manquantes » (le calcul sur la plage du planning est cohérent avec la page), huit mois passés repliés, « Importer un fichier » côté cabinet ; connexion quatre états dont `?expire=1` ; 403 / 404 avec lien par rôle ; admin en cinq blocs, « Actions récentes », « ← Retour à l'app » ; onglets bas à 380 px ; `is_staff` constaté dans Comptes. **Non rejoués** : écran d'import des absences (suites vertes), connexion en principale (compte `TEST Recette` — le rôle « Assistante principale » est un rôle de *Compte*, pas le rôle métier de la fiche).
- **Retours de recette** : effet « vide » du tableau de bord et de l'admin sur 2 560 px, « Ajouter » trop discret → **6a-bis** ; accès à l'administration depuis la page planning → **6d** ; **lisibilité de la grille** (écritures trop petites, cases peu claires, manipulation) → **brique 8**, à cadrer sur le poste réel de la principale (Chromebook 15,6"), pas sur l'écran de Yohan. Ordre des sous-briques modifié : **6d → 8 → 6a-bis → 6b → 6c** (C6.18).
- **Favicon** : dent du logo du site du cabinet, recadrée par Claude conversationnel (Pillow, hors dépôt) sur un carré arrondi au bleu de la charte ; le fichier déposé pesait 14 059 octets pour 8 289 annoncés (PNG ré-encodé au téléchargement, même image 256 × 256) — accepté après constat de Yohan.
- **Reste ouvert** : Phase 6 docs de la 6a (PR séparée : `docs/INTERFACE.md` nouveau, `docs/PLANNING.md`, `README.md`, `CLAUDE.md`) ; captures Chromebook pour la brique 8 ; diff plan 6d.

### Brique 7b — Lecture d'un mois historique — ✅ livrée le 08/09/2026, recettée le 09/09 — brique 7 close
- Branche `brique-7b-lecture-historique` (depuis `main` = `6961fca`), **PR #23**, hash post-squash **`fdfc912`** (commit de branche `0f382ff`), branche supprimée. 6 fichiers, +740 / −3 : `planning/views.py` (+228 : `planning_historique` et quatre fonctions privées ; branche `historique` de `planning_mois` en cinq lignes ; garde de `copie`), `planning/urls.py` (route `historique`), `planning/templates/planning/historique.html` (102 lignes, `extends socle/base.html`), `sans_import.html` (+5), `planning/tests/conftest.py` (la fixture `autouse` substitue désormais `views.charger` en plus de `donnees.charger`), `planning/tests/test_historique_lecture.py` (394 lignes, **22 tests**, dont `test_est_historique_les_trois_cas` — la 7a n'assertait que le cas vrai). **939 tests Python** (917 + 22), **57 Node** inchangés, aucune migration ; `moteur.js`, `donnees.py`, `services.py`, `historique.py`, `admin.py`, `comptes/acces.py` absents du diff.
- **Décisions du 08/09** (deux questions à choix, après relecture de la partie B du diff plan) : journées = jours d'ouverture datés **ou** jours porteurs (réponse « b »), navigation vers `/planning/<mois>/` (aiguillage, jamais un 404 au clic). Deux corrections de ma relecture au plan : les fichiers repris ont `feries` vide → calendrier du socle comme la page ; la navigation calquée sur « Mes jours » aurait pointé sur un `/historique/` inexistant hors 2026.
- **Checkpoint** : trois écarts acceptés — `conftest.py` élargi (sans quoi les 22 tests auraient lu le vrai `regles.json`) ; combinaison des fériés alignée sur `moteur.js::isFerie` ; slot disparu affiché brut comme « Mes jours ». Un écart signalé par Claude Code lui-même : le `_doc` de `periodes_ouverture` (« sert UNIQUEMENT à la paie ») devenait faux → amendé en PR #24. Relecture ligne à ligne du diff **avant** le merge (`diff_7b.txt`, 841 lignes) : imports (`regles.chargeur`, `comptes.noms`, `socle.feries`, rien d'`absences`), 403 / 404, tri D9, règle des journées, log sans nom — un seul écart avec la lettre de la réponse « b » : un férié d'un jour d'ouverture reste une ligne vide marquée « férié » ; Yohan a mergé sans second commit → règle **gardée** et consignée telle quelle en C7.11.
- **Recette en production (09/09)** validée par Yohan sur les cinq points (mars et janvier en lecture, `sans_import` avec « le consulter », `copie` → 404, septembre inchangé, 403 salariée, « Mes jours » jusqu'à janvier) ; captures et logs non versés ici. Déploiement du 08/09 21:32 UTC sans migration.
- **Phase 6 doc du dépôt** — ✅ mergée le 09/09/2026 (**PR #24**, branche `docs-brique-7b`, commit `b2a660b`, hash post-squash **`eec1940`**, 3 fichiers, +110 / −34) : `docs/PLANNING.md` § 1 (route), § 13 retitré « Import et lecture d'un planning historique (7a et 7b) », § 13.7 réécrit (lecture livrée), § 13.8 (limites : dimanche ou férié porteur lisible ici, jamais dessiné par la page vivante), § 10 (piège `conftest.py` des deux `charger`) ; `README.md` ; `regles/regles.json` : une seule phrase du `_doc` de `periodes_ouverture` remplacée, chargement du vrai fichier par `regles.chargeur` vérifié au checkpoint (« 2 périodes, 10 couleurs »). Déploiement Railway de ce merge : à vérifier par `/sante/` (le fichier est chargé au démarrage).
- **Bilan de la brique 7** : quatre PR (#21, #22, #23, #24), aucune migration, 939 / 57, huit mois 2026 importés et lisibles, `verification.py` et le moteur intouchés, `DATA` / `STATE` inchangés.

### Brique 7a — Import historique — ✅ livrée le 08/09/2026, historique en production le même jour
- Branche `brique-7a-import-historique` (depuis `main` = `af2ccce`), **PR #21**, hash post-squash **`95e6ba9`**, branche supprimée. 11 fichiers, +1 643 / −4 ; **917 tests Python** (874 + 43 : 42 nouveaux dans un fichier de tests dédié — forme du fichier, analyse sans écriture, cas D1, écriture, écran d'admin — et 1 dans `test_mes_jours.py`), **57 Node** inchangés ; `makemigrations --check` : aucune migration ; `moteur.js`, `verification.py`, `donnees.py`, `comptes/acces.py` absents du diff.
- **Démarche** : Phase 1 lecture seule (deux découvertes : `DATA` ne sert pas un mois sans import → page dédiée en 7b ; « Mes jours » marche déjà tel quel) → diff plan `diffplan_brique7_import_historique.md` (979 lignes, hors dépôt) en deux parties (A) 7a / (B) 7b → dix points D1 → D10 tranchés par Yohan le 08/09 (C7.7 → C7.10) → Phase 3 en continu → checkpoint diff : deux écarts acceptés (`executer(analyse, qui)` — l'acteur vient de la requête, l'analyse reste pure ; empreinte sur `(mois, state)` canonique) et D1-(1) élargi à **toutes** les versions du mois → merge par Yohan → **relecture ligne à ligne du diff post-merge** (`diff_7a.txt`, `historique.py`, `admin.py`, `audit/models.py`, fixture) **avant** le premier import réel, parce qu'une version historique ne se supprime pas : garde, chemin sans `services.publier`, trois cas D1, audit sans nom — verdict « l'import réel peut partir ».
- **Fichiers d'import** (hors dépôt, chez Yohan, `historique_2026.zip` + `rapport_historique_2026.md`) : huit fichiers `planning_2026-01..08.json` au format export de la page, semaines complètes, produits depuis l'export CSV complet de 📅 Agendas (811 entrées « Présence » de janvier à août, plus les jours de bord) ; **aucune entrée Notion 2026 n'est horodatée** — pas de décalage de date sur l'historique ni sur octobre ; arbitrages de Yohan consignés au rapport : huit demi-journées partagées (brique du matin conservée), samedi 15/08 (Assomption) retiré (garde non tenue), praticien sans assistante = sous-effectif réel conservé, semaine du 31/08 → 06/09 alignée sur la v9 de septembre (une garde Notion du dimanche 06/09 non retenue), briques de Jelly avant le 08/09 ignorées (C7.5) ; fiches praticien **inactives** créées avant l'import : **Seror** (`lisa_ser`), **Krameisen** (`william_kra`) ; Geneviève = `genevieve_aur`.
- **Recette (08/09, production)** : déploiement `main` vert ; pilote **janvier** (analyser → confirmer → rejouer le même fichier → « déjà présente »), puis février → août dans l'ordre ; les huit analyses ont affiché **exactement** les chiffres du rapport — jours porteurs / briques / colonnes : 01 : 21 / 150 / 9 ; 02 : 24 / 174 / 10 ; 03 : 28 / 207 / 10 ; 04 : 23 / 137 / 10 ; 05 : 19 / 139 / 10 ; 06 : 22 / 161 / 11 ; 07 : 18 / 141 / 10 ; 08 : 22 / 109 / 9 — avertissements « fiche close — conservée » attendus sur `william_kra` (février → juillet) et `lisa_ser` (juin), aucun férié porteur, aucune personne inconnue, aucun doublon ; **huit versions historiques** (numéro 1 de chaque mois, affichées 12 → 19 dans la liste de l'admin), **huit événements `planning_historique_importe`** ; ré-import → « déjà présente » ; logs sans nom.
- **Contexte du même jour** (non repris ici) : brique 6 en Phase 1 (rapport `brique6_phase1_inventaire.md`, 2 223 lignes, hors dépôt), dossier interface **v1.2** (C6.11 → C6.16, sous-brique 6b-bis « justificatifs en transit »), brief Claude Design livré, premier retour Claude Design reçu (corrections § 10 du dossier).
- **Phase 6 doc du dépôt** — ✅ **mergée le 08/09/2026** (**PR #22**, branche `docs-brique-7a`, commit `f1af408`, hash post-squash **`6961fca`**, branche supprimée ; déploiement Railway sans migration) : `docs/PLANNING.md` **§ 13 « Import d'un planning historique »** (après § 12 « conflit », sur le modèle d'`ABSENCES.md` § 11 ; +199 lignes), `README.md` (+30 / −11 ; « 825 tests » de la 4b corrigé en 917, route d'admin au tableau § 1, phrase d'état) ; neuf relevés du code sans écart avec le diff plan ; § 13.8 documente la limite « dimanche ou férié ouvert exceptionnellement : importé sans perte, non dessiné par la page » (`moteur.js:79` et `:115`) → tranchée le 08/09 pour la page 7b (une journée fait une ligne si elle est un jour d'ouverture daté de `regles.json` ou si elle porte une brique) ; à consigner en **C7.11** à la Phase 6 de la 7b, avec l'amendement du `_doc` de `periodes_ouverture` dans `regles.json`.
- **Reste ouvert** (au 08/09) : copie du cadrage dans le dépôt à aligner (→ § 12, v1.10) ; 7b (→ livrée, ci-dessus) ; v10 de septembre ; ré-import de fin octobre (C7.6).

### Soirée du 07/09/2026 — conception de la brique 6, cadrage de la 7, octobre dans l'app
- **Vérification de la 3-quater** : 201 absences, PR #19 ; Phase 6 docs déjà faite dans la session précédente (commit `0709356`, **PR #20** mergée en `af2ccce`, 5 fichiers, +210 / −30, 874 tests) ; les entrées « Présence » n'avaient pas été migrées par décision — rouverte par Yohan le soir même (C7.1).
- **Brique 6** : décisions C6.1 → C6.10 par questions à choix ; dossier de conception v0 (`PLANNING_ASSISTANTES_INTERFACE_v0.md`) ; maquettes comparatives de « Mes jours » (trois organisations, puis grille avec codes et marqueurs) ; prompt de Phase 1 (inventaire lecture seule, rapport hors dépôt) livré. Réponses de Yohan sur le rythme : un mois à l'avance, parfois plusieurs.
- **Analyse Notion (lecture seule, agrégats)** : 1 113 entrées « Présence » de janvier à **octobre** (120 / 105 / 118 / 106 / 96 / 117 / 98 / 51 / 146 / 156), colonnes structurées `Praticien`, `Assistante`, `Secretaire`, `Durée` ; septembre saisi du 17/07 au 19/08, octobre du 17 au 30/07, rien depuis le 26/08 ; « WK » (Krameisen) absent du sélecteur, « LS » = Seror ; conversion mécanique.
- **Octobre 2026** : présences 28/09 → 01/11 lues par Claude conversationnel (deux fenêtres, invariant OK, résultat court recopié verbatim) — imports #11 et #12 déclarés **identiques** aux #8 et #9 déjà en base ; fichier `planning_2026-10.json` (212 briques, 35 jours de plage, jours 29/09 → 04/10 repris de la v9), revérifié par une seconde extraction et un second code (171 / 171 briques du 5 au 30/10) ; premier import perdu par rechargement de page (non enregistré) ; **Personnes** corrigées en séance : `TEST Recette` non planifiée et inactive (elle apparaissait dans chaque proposition), **Jaffrenou Adrien** créé, planifié, agenda « Jaffrennou Adrien » apparié (code `adrien_jaf`), Clara 39 h, jours fixes des secrétaires au rythme lundi → vendredi ; écarts signalés par la page et arbitrés par Yohan comme valeurs de départ (une semaine sans l'un des praticiens en semaine 42, ses assistantes passées en sureffectif ; une JC de moins par semaine de cours pour l'étudiante ; un ajustement de secrétariat en semaine 43) ; **v1 enregistrée et publiée**. Règle des étudiantes confirmée (le cours vaut une journée courte, dans les quatre).
- **Jelly** : n'exerce qu'à partir du 08/09 (C7.5) ; « Kelly » = autre personne.
- **Samedi occasionnel** : une garde avec praticien présent fait apparaître la colonne d'elle-même — rien à construire (§ 12 pour le cas secrétariat seul).
- **Reste ouvert** : Phase 1 de la 6 (rapport + captures) → dossier v1 → brief Claude Design → maquettes → diff plans 6a → 6d ; brique 7 pendant les maquettes (fiches Seror / Krameisen à créer d'abord) ; ré-import de fin octobre ; v10 de septembre ; briques 5 et 0.

### Brique 3-quater — Reprise de l'existant 2026 — ✅ livrée le 07/09/2026
- Branche `brique-3quater-import-absences` (créée depuis `origin/main` = `94b376a`), commit `ec9bba4`, **hash post-squash `6de17f1`** (**PR #19** ; Phase 6 docs : commit `0709356`, **PR #20**, mergée en `af2ccce`). 5 fichiers modifiés, 4 nouveaux (+687 / −4 hors nouveaux) ; **874 tests Python** (+49), 57 Node ; aucune migration.
- **Démarche** : Yohan rouvre C3.8 le 07/09 (« tout importer sur l'année 2026, exceptionnellement, puis plus de Notion ») → analyse Notion en lecture seule (1 900 entrées, 446 nominatives, 6 ambiguïtés tranchées : « Lea » = Lea Dehu, vérifié sur 1 113 entrées « Présence » ; Kelly sans fiche écartée ; personne prime sur le titre ; « école » → Ecole ; Clara étudiante jusqu'au 16/06) → décisions I1 → I3 (tous types, écran admin, effectif sans webhook) → **rapprochement avec les bulletins de janvier → août** (70 rubriques, dates comprises) : 15 écarts, règle « la fiche de paie prime » → diff plan v1 → revue technique (1 bloquant : ordre `save()` / calcul ; 14 écarts) → v2 → Phase 3 en continu → checkpoint diff (8 écarts acceptés) → merge → recette.
- **Recette (07/09, production)** : T1 rapport 190 lignes / 7 erreurs = chevauchements internes de mon fichier (fusion trop large, école pendant un arrêt maladie) → fichier v2, 195 / 0 erreur ; T2 195 créées ; T3 ré-import → « déjà présente » partout ; T4 conflits avec la v8 de septembre listés (Clara 01–02/09, Amandine 17/09) puis v9 publiée après retrait de la brique de Lea W du 02/10 (école du 28/09 → 3 briques dans la semaine) ; T5 tir `2026-08` → **7 blocs de CP fusionnés à tort** (jours d'ouverture inventés entre des jours isolés : Lea D 8 au lieu de 6) → 7 + 1 absences supprimées dans l'admin (journalisé), fichier v3 (fusion seulement à travers dimanche, lundi, férié pour les CP ; strictement consécutive sinon), 14 créées, 187 déjà présentes → **201 absences**, Lea D à 5 ; logs sans nom ni type.
- **Réserve** : aucune. Le fichier `absences_2026.json` et les bulletins restent chez Yohan, hors dépôt.
- **Reste ouvert** : ~~Phase 6 docs~~ (faite, PR #20) ; brique 5 avec C5.1 ; brique 0.

### Brique 4b — Planning, publication — ✅ livrée le 07/09/2026
- Branche `brique-4b-publication`, commit `92649d7`, **hash post-squash `0a53bdf`** (PR #17). 24 fichiers modifiés et 7 nouveaux (+671 / −42) ; **825 tests Python** (+47) et **57 Node** (+2) ; une migration en production (`planning.0002`, `RunPython` 0 ligne touchée + `AlterField`).
- **Démarche** : R4a-1 exercé d'abord (06/09 soir) → Phase 1 (rapport de 3 239 lignes, 20 questions ouvertes, écarts au cadrage v1.3 faute de v1.5 sur disque) → décisions Yohan A → F puis P1 / P2 / Q7 → diff plan v1 → **revue technique Claude Code : 7 bloquants** (`deepEqual` d'`importer`, `courant == 0`, `PositiveIntegerField`, crochet admin rejouant à chaque édition, `identite.py` inutile, cycle `donnees`/`services`, fixtures) → v2 → **seconde revue** (cycle `services ↔ webhooks` réintroduit par la v2, `get(mois, numero)`, `Exists` validé sur SQLite en mémoire) → v2.1 → Phase 3 en continu → checkpoint diff (5 écarts nommés, tous acceptés, dont `META.urls.publier` recalculé après un 201, trou du plan) → merge → recette dans la nuit et au matin.
- **Recette (06–07/09, production, personne fictive `TEST Recette`)** : S2 ✓ v4 publiée, `deja_publiee`, v5, v6 ; S3 ✓ « Mes jours » (sa journée seule, italique annoncé), 403 cabinet ; S4 ✓ absence bloquante depuis l'admin → audit `{mois, dates, numero, personne_id}`, mail `absence.conflit` avec `conflits[]`, bandeau ; changement de type → rien (B4) ; S5 ✓ **422 serveur sur enregistrement** (page rendue avant la saisie de l'absence : le serveur voit ce que la page ne voit pas), refus page à la publication après rechargement ; S6 ✓ import du JSON original → « Noemie · hors présence · absent ce jour-là », brique glissée en Sureffectif ; S7 ✓ logs complets sans nom ni type, migration OK. Versions 7 et 8 enregistrées, **v8 publiée**. Nettoyage : compte et personne fictifs **désactivés**, absences fictives supprimées (journalisé).
- **Réserve R4b-1** : le 422 serveur **à la publication** est couvert par `test_publication` mais n'a pas été vu en production (la page l'a refusé avant l'appel).
- **Reste ouvert** : brique 5 (workflow `planning.publie` + variable) ; ressaisie des absences de septembre ; désignation de la principale et invitations réelles ; brique 0.

### Brique 4a — Planning, page servie et versions — ✅ livrée le 06/09/2026
- **PR #15**, commit de branche `13a717e`, **hash post-squash `5072226`**. 10 fichiers modifiés (+134 / −14) et l'app `planning` nouvelle (≈ 9 000 lignes fixtures comprises) ; **778 tests Python** (+127) et **55 tests Node** ; une migration en production (`planning.0001`, table nouvelle, rien d'existant touché). Phase 6 (docs) : branche `brique-4a-phase-6-doc`, commit `71a360e`, **post-squash `8714d52`** (docs/PLANNING.md nouveau, README, CLAUDE.md, formule exacte pour `regles.json`).
- **Démarche** : Phase 1 (rapport de 847 lignes, 29 écarts, 22 questions) → choix Yohan (4a / 4b, C4.1, « mes jours » en 4b) → diff plan v1 → **revue technique v1 : 9 corrections dont 4 comportements faux** (fériés, numéro sous deux workers, école comptée deux fois, absences informatives perdues) → v2 → **revue v2 : 4 corrections dont une destructrice** (`initialise` retiré → re-proposition par-dessus la version enregistrée, C4.3) → v3 → 3-pré (poste resynchronisé, `diff --stat 06eecf9 b243f10` vide, gabarit fictif, proposition figée par Yohan dans Chrome) → Phase 3 en continu → checkpoint diff (13 écarts nommés, tous acceptés, C4.4) → merge → recette le soir même.
- **Recette (06/09, production)** : imports S7 #6 (248 lignes) et #7 (32) ; versions 1 → 3 ; **409 réel** entre deux onglets avec bandeau et « Recharger » ; import d'un JSON bricolé (brique sur un praticien non planifié) → **refus par la page** avant envoi, message composé du code ; copie autonome ouverte hors ligne sans erreur ; logs sans nom ni type. **R4a-1** différé (§ 11) : le fichier de l'assistante était vide — le planning de septembre vit dans un cache de navigateur.
- **Reste ouvert** : ~~R4a-1 ; hygiène `version_de_base` ; cadrage 4b~~ — tout réglé en 4b.

### Brique 3 — Absences — ✅ livrée le 03/09/2026 (3-bis 04/09, 3-ter 06/09)
- **PR #12**, deux commits de branche (`100361e`, `5b2e61b`), **hash post-squash `e2c751d`**. 64 fichiers, +5 736 / −129, 638 tests. **3-bis** (E1) : PR #13, commit `862699e`, **post-squash `d6860a9`**. **3-ter** (E2 + HSTS) : commit `06eecf9`, **post-squash `b243f10`**, 651 tests. Trois migrations en production (`comptes.0003`, `absences.0001`, `absences.0002`), aucune en 3-bis ni 3-ter.
- **Démarche** : Phase 1 d'inspection (01/09, rapport de 7 234 lignes, noms masqués par jetons stables) → diff plan v1 → décisions A–M → v2 → **revue technique de Claude Code : 15 corrections, dont une bloquante sur la formule de paie** → décisions N–P → v3 validé → Phase 3 en continu → **checkpoint diff en trois temps** : migrations et calcul validés ; puis question sur la plage de la paie → **deux défauts trouvés** (semaines complètes au lieu du mois, absence à cheval versée deux fois) → correction avant tout commit → renommage `a_cheval` → merge le 03/09 → recette 04–06/09.
- **Décisions A → P** : § 2 (C3.1 → C3.8) et plan v3 ; en résumé : app `absences` ; `TypeAbsence` en base à l'orthographe Notion ; statut sur l'absence, catégorie sur le type ; formule des jours comptés avec périodes datées et double valeur ; fériés dans `socle` ; rétention fail-closed ; accueil commun ; compte sans personne → message ; paragraphe de paie côté serveur ; client n8n factorisé avec import conservé ; règle K sur la personne ; changement d'adresse par jeton signé ; fixtures remontées au conftest racine (imports dans le corps) ; contrat incomplet → 0 et signal ; demi-journées par correction ; absence réservée aux salariées.
- **Écarts au plan pris par Claude Code, acceptés** : `periodes_ouverture` rendue obligatoire (7 lignes de fixture dans `test_chargeur.py`, aucune fonction de test modifiée) — un défaut d'ouverture silencieux serait pire pour de la paie ; `app_name = "comptes"` (aucun `reverse` ni `{% url %}` nu ne visait ses routes, prouvé par grep) ; `clean_personne` en plus de `save_model` (une `ActionImpossible` dans `save_model` donne un 500 dans l'admin) ; aide `_journaliser_suppression` en 3-ter.
- **Recette (04–06/09, personne fictive `TEST Recette`)** : S1 ✓ cinq cas datés en local ; S2a ✓ demande, webhook `absence.demandee` ; S2b ✓ décision, 3 jours, webhook `absence.decidee` ; S2c ✓ déclaration effective, webhook `absence.declaree` ; S2d → **E1** ; S2e → **E3** ; S3a ✓ paie calendaire 01→30/09, 3 jours, Ecole absente, paragraphe sans type ; S3b **différé** (§ 11) ; S4 ✓ correction à 2,5, paie « 2,5 jour(s) » ; S5 ✓ quatre 401 ; S6a **R2** ; S6b ✓ `details` = `{"statut","personne_id","jours_comptes"}` et `{"mois","nb_salariees"}` ; S7 ✓ ; N ✓ (E2). Hors checklist : refus explicite, trois déclarations dont une de 42 jours (R3), 403 du cabinet sur `/mes-absences/`.
- **Écarts** : **E1** — espace personnel fermé à la principale (défaut du plan v3 § 5) → 3-bis. **E2** — suppression d'absence non journalisée → 3-ter. **E3** — annulation non exercée en production, couverte par les tests.
- **Réserves** : **R1** — `SET_NULL` vu en vrai (§ 5.3). **R2** — export de logs partiel (fenêtre 21:00–21:55 UTC du 04/09 non exportée ; fenêtre couverte propre). **R3** — absence de 42 jours non vérifiable, supprimée avant relecture. Une capture d'invitation reçue en Outlook a montré les liens réécrits par Safelinks ; la recette a basculé sur Gmail (§ 15).
- **Reste ouvert** : étalon de paie (§ 11) ; valeur de rétention (C2.6) ; ressaisie manuelle de l'existant Notion ; ~~identifiant n8n du workflow « Absences (réception) » à reporter~~ (reporté en 4b : `eQABsWqayUIR8KuY`) ; envoi réel des invitations et désignation de la principale ; brique 0.

---

## 15. Leçons de déploiement et de recette

### Railway (1a), Railway et n8n (1b), Tests (1b), Proxy, mesure et recette (2) — inchangées (voir v1.3).

### Aménagement (6a-bis, 12/09 soir)
- **Réduire avant de cadrer** : un retour de recette pris sur le mauvais écran (2 560 px) aurait coûté une brique entière ; en le confrontant à l'utilisatrice réelle (1 536 px), il s'est réduit à trois points utiles partout.
- **Une règle « nouvelle » peut déjà être garantie par le code** : « un import réussi couvre toute sa plage » semblait une décision produit ; la revue a trouvé la garde à l'import (`lecture.py:185-193`) — la relire avant de la décider.
- **Le type d'une clé est un comportement** : un `set[date]` là où le consommateur teste `jour.isoformat()` rend tout « manquant » sans lever d'erreur ; la revue technique contre le code l'a vu, les tests existants l'auraient vu après coup.
- **Mesurer le coût d'une page par un test temporaire hors index** (`CaptureQueriesContext`, N croissant) donne la formule, pas seulement un chiffre ; le fichier disparaît avec son `.pyc`.
- **Un script de la barre vit avec la barre** : le bloc `scripts` d'une page peut être redéfini sans `block.super` ; ce qui doit être partout se pose là où est le balisage qu'il sert.

### Grille (8 et 8-bis, 12/09)
- **Une grammaire visuelle se juge sur les données réelles, pas sur le dessin** : « pâle = proposée par le moteur » était cohérent sur le papier et rendait tout pâle en production, parce qu'un planning publié sans retouche garde son drapeau « proposée ». Regarder d'abord ce que portent les données du mois en cours.
- **La revue technique du diff plan contre le code, avant la Phase 3, paie** : six corrections en une lecture, dont deux comportements faux que les tests n'auraient pas vus (une règle CSS d'auteur bat `[hidden]` ; un liseré ambre sur une encre sombre est invisible).
- **Un dernier lot détachable coûte une commande** : lots précédents dans l'index, dernier lot dans l'arbre de travail — `git diff --cached` / `git diff` au checkpoint, `git checkout -- .` pour le retirer, commit unique dans les deux cas.
- **Vérifier `.env` avant tout serveur local, sans l'afficher** : la présence de `DATABASE_URL` peut vouloir dire production. Et un accès par lien magique rend les captures locales impraticables : prévoir la recette en production, ou un compte de test local.
- **`overflow:hidden` neutralise `position:sticky`** chez tout descendant ; `overflow:clip` rogne sans créer de conteneur de défilement.
- **Des branches squash-mergées se prouvent par l'égalité d'arbre** (`%T` d'un commit de `main` = arbre de la pointe), pas par `--merged` ; suppression en deux temps, lecture puis `-D` sur go.

### Enveloppe (6d, 11/09)
- **La garde par le diff marche** : demander au checkpoint une table des hunks (ligne d'origine → sélecteur ou fonction → autorisé par quel paragraphe) rend vérifiable en une lecture qu'un fichier partagé (`styles.css`, `page.js`) n'a été touché que là où le plan le permettait.
- **Une décision de conception se renverse en une capture** : le sélecteur « Planning individuel » (C6.12, décidé le 07/09 sur maquette) a été jugé redondant à la première vue en production — livrer petit, recetter vite, ne pas défendre une maquette contre l'usage.
- **Des captures d'émulation suffisent à mesurer** (hauteurs, largeurs, passage à la ligne) mais pas à juger : les captures du poste réel restent nécessaires avant de toucher à la grille.
- **Une feuille partagée doit être autonome** : une barre commune qui dépend de règles générales d'une autre feuille casse sur la première page qui ne charge pas cette feuille.

### Coquille (6a, 10/09)
- **Un processeur de contexte tourne sur chaque page, y compris celles qui n'utilisent pas ses variables** : garde anonyme obligatoire (`AnonymousUser` n'a pas les attributs d'un compte) et valeurs paresseuses, sinon une requête SQL de plus partout — les coûts se figent par `assertNumQueries` avant l'implémentation.
- **L'ordre d'`INSTALLED_APPS` est l'ordre de recherche des gabarits** : une surcharge d'`admin/base_site.html` dans une app placée après `django.contrib.admin` n'est jamais trouvée, sans erreur. Un gabarit peut étendre le gabarit natif du même nom (Django saute l'origine courante).
- **Après un squash-merge, `git branch -d` refuse toujours** ; `-D` seulement après `git rev-parse <branche>^{tree}` = arbre du commit squashé.
- **Un fichier binaire transmis par un navigateur peut être ré-encodé** (PNG de 8 289 → 14 059 octets, même image) : vérifier dimensions et rendu plutôt qu'une empreinte annoncée.
- **L'écran de la recette n'est pas l'écran de l'utilisatrice** : tout ce qui paraît « vide » à 2 560 px est plein sur un Chromebook de 15,6" ; une brique d'interface se cadre sur des captures du poste réel.
- **La relecture du diff avant commit paie encore** : une règle CSS sur `button` nu aurait rendu identiques « Valider » et « Refuser » sur une page que la 6a ne touchait pas.

### Conception et bascule (07/09, soir)
- **Vérifier « ce qui a été fait » avant de concevoir** : la relecture du journal a montré que les entrées « Présence » n'étaient pas migrées et que Notion portait encore deux mois de planning ; sans cette lecture, la brique 6 aurait été conçue pour une principale qui n'utilise pas l'app.
- **Un plan de juillet confronté aux données d'octobre parle** : praticienne absente une semaine, quotas, colonne manquante — ce sont des décisions de planning, pas des erreurs de conversion. Le fichier reste fidèle à la source ; la page fait son travail ; on ne « corrige » jamais la source pour faire taire la page.
- **Un résultat d'outil court se recopie verbatim et se prouve par l'invariant** ; un résultat long se force sur disque et se lit par script — jamais de recopie manuelle de données.
- **Une page qui fusionne jour par jour garde ce qu'on ne lui envoie pas** : couvrir explicitement toute la plage, jours fermés vides.
- **Les décisions produit se prennent par questions à choix, une brique à la fois** ; le redesign a coûté neuf questions et deux maquettes, pas une ligne de code.

### Lecture d'un mois historique (brique 7b, 08–09/09)
- **Un mot du langage courant n'est pas une règle** : « jours ouvrés » a trois lectures possibles dans l'app (lundi → samedi, jours d'ouverture datés, jours porteurs) ; la réponse à choix a été traduite en règle exécutable *avant* le prompt, et dite à Yohan pour objection — c'est ce qui a permis de voir, à la relecture, le seul cas où le code s'en écartait (le férié d'un jour d'ouverture) et de le trancher en connaissance de cause plutôt que de le découvrir en recette.
- **Une décision de lecture peut rendre une doc de configuration fausse** : Claude Code a levé lui-même le `_doc` de `periodes_ouverture` ; le réflexe « qu'est-ce que ce choix contredit ailleurs ? » vaut d'être posé à chaque décision de cadrage, pas seulement au code.
- **Le chiffrage d'un plan compte le code, pas les docstrings** : +228 lignes de vue pour +70 planifiées, sans logique cachée — la relecture avant merge est ce qui distingue « plus long que prévu » de « autre chose que prévu ».

### Import historique (brique 7a, 08/09)
- **L'idempotence se juge sur le contenu canonique, pas sur les octets** : un ré-export de la page change `exporte` sans changer le planning ; l'empreinte sur `(mois, state)` nettoyé est ce qui rend « déjà présente » vrai.
- **Ce qui ne se supprime pas se relit avant d'entrer en production** : une version historique ne se corrige que par une version suivante ; d'où la relecture ligne à ligne du diff post-merge, puis un **pilote sur un seul mois** (analyser, confirmer, rejouer) avant les sept autres.
- **Une analyse chiffrée à l'avance transforme la recette en comparaison** : les jours, briques et colonnes attendus par fichier étaient écrits avant le premier clic ; un écran qui aurait dit autre chose se serait arrêté là.
- **Un mois « vivant » se protège sur toutes ses versions** : refuser l'import historique dès qu'une version non historique existe, quel que soit l'ordre, évite d'écraser un travail de la principale par une reprise d'archive.

### Reprise d'un existant (brique 3-quater)
- **Une page par jour dans la source impose une fusion, et une fusion se prouve contre les jours réels.** Deux règles de fusion fausses (écart ≤ 3 jours, puis « jours de fermeture » appliquée à tous les types) ont inventé des jours d'absence ; la bonne règle : ne souder qu'à travers des jours de fermeture, et seulement pour les congés payés.
- **La fiche de paie est l'étalon, pas la saisie.** 15 écarts Notion ↔ bulletins sur 450 entrées, tous tranchés par le bulletin ; sans lui, on aurait importé une maladie un lundi fermé et un décès sur la mauvaise personne.
- **« Déjà présente » est la meilleure preuve d'un import** : un ré-import intégral qui ne crée rien dit que fichier et base coïncident. L'idempotence a servi trois fois en une journée.
- **Un rapport qui refuse tant qu'il reste une erreur oblige à corriger la source**, pas la base : les trois séries d'erreurs ont été réglées dans le fichier, jamais par une retouche manuelle en base (hors suppressions journalisées).
- **Un étalon inattendu vaut mieux qu'un étalon attendu** : le workflow Notion n'a jamais été retrouvé ; huit bulletins l'ont remplacé et ont tranché une décision de cadrage (C5.1).

### Recette croisée et double vérification (brique 4b)
- **Le planning vécu se sauve à la première occasion.** R4a-1 a tenu deux semaines dans un `localStorage` ; l'exercer avant la 4b a produit une violation réelle, un écart de page réel (E1) et la version 4 — tout ce que le générateur brut ne pouvait pas donner.
- **La seconde revue vaut la première.** La v2 corrigeait le cycle `donnees`/`services` et en réintroduisait un autre (`services ↔ webhooks`). Une revue par version de plan, contre le code, jusqu'à ce qu'elle ne trouve plus rien.
- **Un essai ORM tranche mieux qu'une phrase de plan.** « Forme de l'`Exists` acceptée par Django 5.2 » a coûté trois lignes sur SQLite en mémoire et a sorti le point de la Phase 3.
- **Deux ordres, deux gardes.** Une absence saisie *avant* publication est refusée à la publication (C4.6) ; saisie *après*, elle est signalée (C4.7). La recette a montré un troisième cas : la page rendue *avant* la saisie laisse poser une brique que le serveur refuse — la double vérification (§ 6.4) n'est pas redondante.
- **Un onglet ouvert est un instantané.** Recharger le planning après toute saisie d'absence, jusqu'à ce que la page se rafraîchisse seule (§ 12).
- **Un identifiant de workflow se reporte le jour où on l'a sous les yeux** : `eQABsWqayUIR8KuY` attendait depuis la 3.

### Page, brouillon et référence (brique 4a)
- **La copie autonome sans version serveur était une perte de données en attente.** Le planning réel de septembre a tenu un mois dans le `localStorage` d'un navigateur, jamais écrit sur disque. La 4a est la réponse structurelle ; tant que R4a-1 n'est pas exercé, ce planning n'est sauvegardé nulle part.
- **Un contrat allégé se relit jusqu'au dernier lecteur.** Retirer `initialise` du `state` était juste ; oublier que la page décidait de proposer dessus aurait écrasé chaque version enregistrée au rechargement. Règle : toute clé retirée d'un contrat se cherche par grep chez tous ses lecteurs, et la décision qu'elle portait se relocalise explicitement (C4.3).
- **Une revue par implémentation, pas par plan** : les deux revues de Claude Code contre le code réel ont trouvé treize défauts que le plan ne voyait pas, dont cinq à comportement faux. C'est désormais une étape obligatoire entre v1 et v3.
- **Deux implémentations d'une même règle s'alignent par un jeu de cas commun**, pas par relecture : `cas_verification.json` lu par pytest et par `node:test`. La divergence « férié samedi » aurait été rouge dès le premier tour.
- **Un résultat de référence se fige depuis l'ancien code, à la main, une fois.** Le gabarit d'origine ne tourne pas sous Node ; sans la proposition exportée depuis Chrome par Yohan, le test aurait comparé le moteur à lui-même.
- **Sous gunicorn à deux workers, la contrainte unique est le seul point de sérialisation** (pas de `select_for_update` en SQLite de test) ; `except IntegrityError` hors du `with atomic()`, sinon la relecture lève.
- **`node --test` prend un motif glob**, pas un répertoire ; `json_script` remplace tout échappement manuel ; `finders.find` pour inliner un statique en production manifest.

### Paie, audit et recette (brique 3)
- **Des tests verts ne prouvent qu'un comportement spécifié.** 606 tests étaient verts sur une paie qui facturait deux fois une absence à cheval, parce que le plan ne disait pas sur quelle plage filtrer et que tous les tests de paie tenaient dans un seul mois. La question « sur quelle plage ? » posée au checkpoint diff a fait plus que la suite de tests. Règle : pour tout calcul lié à de l'argent, un test qui traverse une frontière (mois, semaine, régime) est obligatoire.
- **Ne jamais recouper une absence pour relancer le calcul.** Le plafond hebdomadaire s'appliquerait une fois par morceau (27 h à cheval : 5 au lieu de 3). Figer des **dates** au calcul, puis répartir sur les dates, évite le piège par construction.
- **`plage_mois` est l'outil du planning, pas de la paie.** Semaines complètes d'un côté, mois calendaire de l'autre ; deux fonctions, deux noms.
- **Le `SET_NULL` vu en vrai** : supprimer un compte efface l'auteur de tous ses événements d'audit en une seconde. Désactiver, jamais supprimer — et corriger structurellement (§ 12, tête de liste).
- **Une action d'admin de liste ne se trouve pas sur la fiche.** « Créer les comptes » et « Envoyer une invitation » se font depuis la liste, case cochée, menu « Action ».
- **Les liens à usage unique et les scanners de messagerie** : Outlook/Safelinks réécrit les liens et peut les pré-visiter ; recetter en Gmail, et prévenir les salariées dont la messagerie fait de même.
- **L'export Railway plafonne, et un redéploiement coupe la fenêtre** : exporter par déploiement, avant de merger la sous-brique suivante. Et vérifier le **service** exporté — un export du serveur MCP a été fourni par erreur, ce qui a au passage révélé que ce serveur journalise une adresse en clair (§ 12, hors projet).
- **Renommer un champ avant qu'il ait un consommateur** : `a_cheval` → `facture_partiellement` a coûté cinq minutes avant le premier merge ; après la brique 5, c'était un changement de contrat.
- **Une `ActionImpossible` dans `save_model` de l'admin donne un 500** : la garde métier va dans le `ModelForm` (`clean_<champ>`), `save_model` reste le dernier rempart.
- **Amender une migration initiale est légitime tant qu'elle n'a jamais été appliquée en production** ; `0002` qui en dépend doit être mise de côté le temps de régénérer.
- **`pytest.ini` porte déjà `-q`** : un second `-q` masque le résumé.
